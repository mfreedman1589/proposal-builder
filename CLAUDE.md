# Proposal Builder

A Streamlit app that assembles polished, client-personalized Premion CTV/OTT proposal decks from a form, by deleting unselected slides from a master `.pptx` and filling the retained template slides. The Claude API drafts a first pass at the form from pasted meeting notes and powers the audience-segment finder; Supabase holds the master deck, the rate card, the audience catalog, the case study vault and every logged proposal.

**Stack:** Streamlit (form + UI), python-pptx (deck assembly/fill), pdfplumber (audience catalog PDF parsing), Anthropic Claude API (`claude-sonnet-4-6` -- draft-from-notes + audience suggestions), Supabase (Postgres + private storage buckets), PowerPoint over COM (local rendering and validation only).

## Where things are written down

This file is loaded into context at the start of **every** session, so its size
costs tokens on every turn. It carries the rules and the traps — not the
reasoning behind them, and not the history.

| Doc | What's in it |
| --- | --- |
| `CLAUDE.md` (this file) | The rules that govern a change, the traps that cause a bug if forgotten, how to run things, current state. |
| `DECISIONS.md` | Why each rule exists — the incident, the measurement, the alternative that was tried and abandoned. Every rule below marked → `DECISIONS.md` has its full original wording there. |
| `HISTORY.md` | What was built in what order, and what each round of live testing turned up. |
| `BACKLOG.md` | Living list of queued feature work — what each item is, why, and what's already decided. **Record a new feature idea here as it comes up**, rather than losing it or re-litigating it from scratch next time it's raised. |
| `SLIDE_KEYS.md` | Slide tagging: how a `key:` label works, how to correct one, how the update page gates on it. |
| `DEPLOYMENT.md` | Streamlit Cloud: secrets, deploy order, schema-before-push, warm-process staleness. |
| `proposal_builder_build_spec.md` | The full spec (data model, form fields, slide map, phasing). Read it before making non-trivial changes. |
| `master_v1_1_changelog.md` | The master deck's token conventions and what changed from v1. |
| `geo_targeting_roadmap.md` | Geo/zip/market targeting: what's built, what's researched, the avails-PDF import, the `audience_usage` analysis. |
| `market_profiles_index.md` | The extracted market viewer profiles. |
| `master_deck_slide_keys.md` | Generated slide → title → key reference. A convenience copy; the speaker notes are the source of truth. |

**Keep this file under 150k characters, and prefer well under it.** When a note
here grows past the rule into the story behind it, move the story to
`DECISIONS.md` (or `HISTORY.md` for build history) and leave one line naming the
rule and its guard. When a rule ends up enforced by a test, the line here names
the rule and the test, and the reasoning belongs in that test's docstring.
Adding to this file is not free — the default is to move something out, not to
append.

## Key conventions

### Data, the backend and the rate card

- **All Supabase access goes through `db.py`, and every loader falls back rather than raising.** Nothing else imports the supabase client. Every *read* returns `(value, warning)` — real data or the local fallback, plus `None` or a plain-language sentence the caller shows with `st.warning`. **A fallback is never silent, and a loader never raises for an unreachable backend**: the form has to render offline, on a broken URL, on a paused project, and in local dev with no secrets. *Writes* return `(ok, error)`; a failed `log_proposal` is a caption, not an error. A loader returns `None` rather than `[]` when the caller should fall back. Catalog reads are paged past PostgREST's 1000-row default. What falls back to what: master deck → the checked-in `.pptx`; rate card → `FALLBACK_PRODUCTS`/`FALLBACK_SPORT_CPM` in `app.py` (**edit rates in Supabase, not there**); audience catalog → parsing the brochure PDF. → `DECISIONS.md`
- **DDL is not something the app can do, and schema goes before code.** Statements live in `supabase_schema.sql`, are idempotent, and are pasted into the Supabase SQL editor by hand. The sequence is **run the SQL → push → confirm the live app is serving the new code**, rebooting it if it isn't — a push that succeeded is not proof the new code is running. `setup_supabase.py all|deck|products|audiences|case_studies|slide_vault|proposal_files` does everything the client can; `products` is a **bootstrap, not a sync** (re-running it overwrites rate edits made in Supabase). Every table has RLS enabled with no policies; the app is server-side on the service key. → `DEPLOYMENT.md`
- **Sports are products.** `products` rows keyed `sport:<sport_key>` with `display_group='sports'` carry the per-package ratecard rates, so `line_product_spec` resolves a sports line's label and CPM through the same one lookup as everything else. `line_type` is `rate`/`flat` (matching the media-plan grid's Type column); the old premion/am/broadcast value is `display_group`, which is what it always actually meant. `targeting_copy` is the product's own column rather than two hardcoded prefix rules, matched by tactic-name prefix (longest first, since product labels are prefixes of one another) so a drafted row with a label appended still picks it up.
- **The audience catalog lives in Supabase's `audiences` table; the brochure PDF is the local fallback, not the live source.** `audience_catalog.load_audience_catalog()` reads `db.fetch_audiences()` first; `_parse_pdf_catalog()` (segment/category/subcategory/RFP-selectable, RFP-selectable detected from the highlight *rectangle*, a fill shape, not a text colour) only runs when Supabase is unreachable. If the brochure is replaced, re-verify against page 2's own roster and read the two PDF-geometry gotchas first. → `DECISIONS.md`
- **The YTD usage workbook Matt refreshes periodically is an uploadable, versioned data source, the `deck_versions` pattern applied to audience data — "Update audience usage."** `audience_usage_import.py` (pure, no DB) parses the workbook (Segment Name / Delivered Impressions, one row per booked stack; "No Data Targeting" dropped) and **merges** the result into the catalog rather than replacing it: components are split on commas (a stack is an AND-list, same derivation `audience_usage_ytd.csv` always used), folded through `market_profiles._normalize` before merging so a case/punctuation variant can't split one segment's history in two, and categorized from their own prefix or, for a `CUSTOM`-prefixed one, a second prefix embedded in the name (`"CUSTOM FOOD Grocery Delivery Services"` → `FOOD`) — never guessed past that; an unrecognized prefix is reported uncategorized. **A component that normalizes to an existing catalog segment keeps that segment's exact spelling, category and `rfp_selectable`** (the brochure is authoritative for RFP-selectable, which `custom_segment_count` depends on) **and only gets its usage numbers refreshed; a component the catalog lacks becomes a new row, `rfp_selectable=False`** — the safe default. **`CLT`-prefixed components (a client's own first-party data, never selectable for anyone else) are excluded from the catalog outright**, and from `audience_evidence`'s booking-evidence index too (`_split_parts`/`evidence_for` both filter them, so one can never surface as a suggested pairing) — the raw `audience_usage` transcript keeps the full original stack text regardless, since exclusion is enforced at every consuming boundary, not by rewriting the historical record. `audiences.metrics` is a jsonb bag (`{"impressions": N}` today) so a second ranking metric (a response rate, later) is a new key, not a migration; `prioritize_catalog` and the finder's familiarity line both rank by impressions now, not `times_used` — **stack count is kept where the evidence panel is about precedent** (co-occurrence, pairings) rather than volume. `db.activate_audience_usage_version` does the actual rebuild (unlike `activate_deck_version`, a bare flag flip won't do — nothing else re-derives these tables on a refresh); the admin page parses and reports gained/collided/uncategorized/excluded-CLT counts client-side before any write, the same upload → validate → gate → activate shape `render_update_master_deck` uses. Guards: `tests/test_audience_usage_import.py`, `tests/test_update_audience_usage_page.py`, `tests/test_audience_evidence.py`'s CLT section.
- **A second class of exclusion, same treatment as CLT, for components with no CLT prefix: one advertiser's own retargeting/address-list pool.** `is_client_pattern` catches four structural forms mechanically (`WEB RT`, `LOCATION RT`, `..._RFPID-..._RT`, an "Address List" suffix — a search, not an anchored match, since the real data has both `"WEB RT- X"` and `"X _Web RT"`); verified against a real 106-component markup pass at zero false positives, catching 29 of 41 confirmed cases automatically. The rest — a bare client/campaign name with no structural marker (`"CUSTOM Out West"`) — can't be told apart from a real custom segment by pattern, and live in `audience_component_overrides.csv` (`action=exclude_client`) instead, alongside three more manual actions the same registry holds: `categorize` (added `B2B` and `LEGAL` as categories this way — confirmed `LEGAL` doesn't collide with the brochure's own `FIN Legal Services`, a different segment entirely, still `FIN`, untouched), `split` (an OR-joined compound that's really two components, each getting the FULL stack/impressions attribution of the whole phrase — the same rule two comma-separated components in one real stack already follow, never divided), and `leave_blank` (confirmed uncategorizable, so it stops re-surfacing in every future report). `load_overrides()` reads the registry automatically on every `derive_catalog_updates` call, so this is permanent pipeline behavior, not a one-time script — a future workbook needs a manual pass only for what the pattern and the existing registry both miss. `audience_evidence.py` gets its own copy of the mechanical pattern only (not the registry — importing it would pull in db.py/streamlit through `audience_catalog`, against that module's "no DB, no Streamlit" design), so the ~12 registry-only exclusions are catalog-scoped, not evidence-index-scoped — a known, explicit boundary.
- `.pptx` and `.pdf` files are gitignored (master deck, generated test output, source brochure) — never commit them. Secrets live in `.streamlit/secrets.toml` (gitignored) — `APP_PASSWORD` (shared-password gate) and `ANTHROPIC_API_KEY` (draft-from-notes + audience suggest, read via `st.secrets.get("ANTHROPIC_API_KEY")` at runtime). That file is fenced off hard: `.claude/settings.json` denies reading it via any tool, and the sandbox itself blocks even a blind existence/append check on it from this assistant — the user edits it directly.

### Deck assembly and slide selection

- **Slide map is always derived by scanning, never hardcoded.** `slide_map.py` opens the master deck and classifies every slide into a `condition_key` (e.g. `vertical:healthcare`, `vertical:healthcare:targeting`, `sport:nfl_playoffs`, `sport_viewership:nfl_playoffs`, `full_deck`, `transitional_divider`) by matching text anchors, with a small carry-forward default for undifferentiated divider slides. If the master deck is replaced with a new version, do **not** hand-patch slide numbers — update the anchor rules in `slide_map.py` if a new slide type needs one, then let it re-scan. This was a direct, hard-learned lesson from the v1 → v1.1 deck migration (slide count and positions shifted; a stale hardcoded map would have silently mis-assembled decks). Watch for anchor collisions with boilerplate footer text (e.g. "CONF" matching "confidential") — needles should be as specific as the real text allows.
- **A slide's `key:` label in its speaker notes beats the text anchors.** `slide_map.build_slide_map_from_prs` reads it first and falls back to anchors only for an untagged slide; matching is case-insensitive and whitespace-tolerant, and the **last** `key:` line wins. Anchors still run on every slide, because the carry-forward section default has to keep tracking. Correcting a mislabelled slide means editing its notes, not the code — which is exactly why the labels live in the deck. → `SLIDE_KEYS.md`
- **The "Update master deck" page blocks on a slide whose key came from carry-forward**, not on `key is None` — anchor classification always produces *some* key, so a naive gate would never fire. `_classify_all` reports a source per slide (`notes` / `anchor` / `carry`), and `carry` is a guess dressed as an answer. → `SLIDE_KEYS.md`
- **No part of the app depends on slide numbering.** Presets select by condition_key (`CORE_CONTENT_KEYS`, `STANDARD_FORCED_KEYS`), never by slide number — the old literal allowlist would have mis-assembled every deck after the first master update. **"Standard" forces the avails template in even when the form's toggle is off**, so targeting/avails mutual exclusion is enforced as a **post-processing step in `build_presentation`** based on which slides actually ended up kept, never on the toggle value alone: whenever the personalized avails template is present, the vertical's static Precision Targeting slide is dropped, in every preset. → `DECISIONS.md`
- Slide deletion works by removing the `<p:sldId>` from `presentation.xml`'s `sldIdLst` and dropping the corresponding relationship (`prs.part.drop_rel`) — python-pptx then automatically omits any part unreachable from the package's relationship graph on save, so orphaned slide parts clean themselves up. Reordering (e.g. moving the vertical stats slide to position 3) is the same idea: remove the `<p:sldId>` and `.insert()` it back at the target index.
- **No section dividers appear in a generated proposal, ever.** All ten classify as `slide_map.SECTION_DIVIDER_KEY` and `build_presentation` drops them as a post-processing step, not as an omission from `resolve_active_keys`. Each entry still carries a carry-forward default, which is what resolves the slides it introduces. → `DECISIONS.md`
- **Total TV drives the deck's branding on its own, with no extra toggle**, and the market picks every branded variant — cover, plan template, and the station's own schedule slide. Replacement is a **sweep in `build_presentation`**, and both markets' variants are swept unconditionally: a competitor station's branding in front of a client can't be allowed to survive a selection bug. **Three of the seven variants resolve from their notes label ALONE** (they are text-identical to what they replace), so a merge that drops speaker notes collapses them silently. Guards: `check_total_tv_by_market`; `tests/test_draft_regression.py` re-applies the labels after `copy_slide_into`. → `DECISIONS.md`
- **A sport contributes its package slide; the viewership chart is opt-in** via one deck-wide `products.live_sports.viewership` checkbox covering every selected sport. `sports.get("viewership", True)` is the one place a missing key means True — only proposals logged before the toggle existed carry none, and "Rebuild as presented" has to reproduce what the client received. Same shape as `vertical_attribution: None`: when a new selection field changes what a *rebuild* produces, the legacy value has to be the old behaviour, not the new default. → `DECISIONS.md`
- **"Working from an avails document" (`avails_mode`) is authoritative over everything downstream of it, not just the uploader — a real WAEPA find (2026-09-08).** With the toggle off, a proposal still: seeded a placeholder targeting group from a drafted audience (`apply_draft_to_form`'s seeding branch now checks `avails_mode` and, when off, routes the audience name to `unresolved_internal` instead of seeding); seeded a D2 placeholder row per target market (the D2 gate now reads `include_avails_template and avails_mode`); and kept `targeting_avails_template` in the deck even though `STANDARD_FORCED_KEYS` forces it in for the "standard" preset — `build_presentation` now drops any `TARGETING_AVAILS_KEYS` slide whenever `selections.get("avails_mode", True)` is False, the same drop-after-the-fact sweep shape as the Total TV/attribution sweeps below (and the report builder's delivery-set drop), placed before the mutual-exclusion check so a vertical's own static fallback can still surface. A `selections` dict with no `avails_mode` key (every caller that predates this) defaults True, unchanged. Hand-typing with the toggle ON and no document is still today's intended behavior — a placeholder row a rep fills by hand. Guard: `tests/test_avails_mode_authoritative.py`. → `DECISIONS.md`
- **Auto and travel sell their own attribution product (Polk Signals / Arrivalist), and it stands in for the generic line.** `assembly.VERTICAL_ATTRIBUTION` plus a per-vertical Section D checkbox (`vertical_attribution_auto`/`_travel`, checked by default) drives the slide *and* the Included-with-Campaign line. **Auto replaces the generic "Measure Sales Conversions" slide; travel doesn't** (`replaces_sales_slide`) — that asymmetry is deliberate, not an oversight. Matched by a text marker and swept in `build_presentation`, the same mechanism as the Total TV variants. → `DECISIONS.md`
- **A master deck is optimized on the way into storage, and blocked if it still won't fit.** `optimize_deck.py` re-encodes opaque PNGs as JPEG (~95MiB → ~48MiB against a 50MiB ceiling), leaves genuinely transparent images, vectors and embedded fonts alone, and **is idempotent — keep it that way**, since a deck round-trips repeatedly. `db.prepare_deck_for_upload` sits in front of every upload route and writes nothing (no file, no version row) when the optimized deck is still too big. Verify a new build with `--verify`. → `DECISIONS.md`
- **Case studies go immediately before the media plan slide, and the graft must happen before `personalize`.** The insert point is found by the `{{PLAN_TITLE}}` token — not by condition_key, which Total TV's plan variants broke — and `personalize` both consumes that token and clones the plan slide per option directly after the original. Nothing is ever included silently: vertical matches are pre-checked but always visible and overridable, off-vertical ones sit in an expander, and the `cs_pick_*` keys are deleted when the vertical changes. → `DECISIONS.md`
- **Slide duplication is how multi-option decks are built, and rIds are the whole trick.** `assembly.duplicate_slide` adds a slide from the same layout, deletes the auto-cloned placeholders, copies the shape tree and rewrites every relationship reference (`_remap_relationship_ids`), because rIds are **part-local**; the layout and notes-slide rels are deliberately excluded. **And a duplicated slide needs a partname nobody else is using** — after `build_presentation` has deleted most of the master, python-pptx's `slide{len+1}.xml` collides with a retained slide, **nothing raises**, the only symptom is a `zipfile` `UserWarning`, and one part silently overwrites the other. `_next_free_slide_partname` claims a genuinely free number first. Invisible to a synthetic test deck, whose slides *are* contiguously numbered. → `DECISIONS.md`
- **Token fill convention:** template slides carry `{{TOKEN}}` placeholders in run text. Fill by finding the run containing the token and doing `run.text = run.text.replace(placeholder, value)`. **Never assign `text_frame.text`** — it collapses all run-level formatting (font, color, size) on that paragraph. Multi-item content (bullet lists, table rows) is filled by deep-copying the tokenized paragraph/row XML element and inserting clones as siblings, then filling each clone the same way. See `fill_bullet_list` / `fill_table_rows` in `assembly.py`.

### The media plan and the form

- **A product toggle diffs the plan; it never rebuilds it.** Rows are added only for newly-selected products and removed only for deselected ones (`_seeded_tactics`, matched by prefix since a drafted row appends its own label); **every existing row is left alone**, drafted or dirty or otherwise. Additions are guarded by what's on the grid rather than by the stored list, because a draft and a rehydration both write rows without it. This is the second rerun path that silently zeroed drafted rows, which is why `check_post_draft_edits` tests the whole seller sequence — draft, toggle on, import, toggle off. **Premion Streaming TV is the one exception to this whole mechanism** — see "Targeting groups own their Premion Streaming TV line" below, `_seed_option_rows` forces it off before this diff ever runs. → `DECISIONS.md`
- **Targeting groups own their Premion Streaming TV line — the avails table is research, never an implicit seed list.** Every group defaults `include_in_plan=False` (`targeting_groups.new_group`, same shape as `color`/`color_locked`: a value plus "a rep broke this one out, stop auto-managing it" — `include_locked`), whether it arrived via an avails-PDF import, the Audience finder, or a hand-typed D2 row. A group's line exists if and only if the D2 grid's **Plan** checkbox column (first visible column, same `_cell_unchanged` fold-back discipline every other D2 column uses — the highest-risk line in the whole feature is `tg.new_group` losing `include_in_plan`/`include_locked` on a render that touched nothing) is ticked — one at a time, via **"Add all to plan"**, or pre-ticked by a draft (below). `reconcile_group_plan_lines` (app.py) is the single, idempotent owner: add a row for a newly-selected group, remove one whose group was deselected (silently if clean, **queued for confirmation if the row is dirty** — a hand-edited line is never discarded without naming the numbers and requiring **"Remove them and discard those numbers"**; **"Keep them on the plan"** just drops the queue), re-price selected clean rows against a stored allocation on request. All four D2 actions (a single tick, Add all, Clear plan lines, Remove/Keep) go through the SAME queue-then-apply-before-D2-reads-groups shape `apply_pending_color_cascade` already used — a button that both reads and writes `_pending_include_removal` inside its own `if st.button(...)` block reads it before that render's fold-back has run and writes it after, so the fold-back's own merge is already stale by the time the write lands (a real bug, caught by `tests/test_group_plan_selection.py`'s Keep/Remove flow going red before the buttons were moved onto the shared mechanism). Every **other** product (retargeting, AM, sports, fees, broadcast) is untouched by any of this and produces **one line for the whole campaign**, not one per group — never fanned out per targeting group to begin with; `_seed_option_rows` passes the single flat `default_geo`, not the per-group `plan_lines`, for anything that isn't Premion. Guards: `tests/test_group_plan_selection.py` (the mechanism, fold-back survival, idempotency), `tests/test_group_plan_order_invariance.py` (below), `tests/test_avails_pdf_wiring.py`.
- **Drafting selects from existing groups instead of inventing lines, and order stops mattering once real groups exist by the time selection is expressed.** `build_draft_prompt`'s `existing_groups` parameter (populated from `session_state["targeting_groups"]`) adds a prompt section — and three new schema keys, `group_selection`/`group_allocation`/`group_selection_reason` — **only when real groups already exist**; a no-groups draft's prompt, and every frozen tier-1 fixture, is byte-identical to before. `apply_draft_to_form` computes `groups_own_plan` once, from session_state as it stood **before** this draft, and when true **never writes `avails_seed_rows`/`targeting_groups` from the drafted audiences at all** (the fix for the bug that made import-then-draft destroy a real import's resolved `geo_def`/`resolved_zips` — line 3556's old wholesale overwrite). `match_groups_to_selection` resolves the model's `ids`/`match` phrases against exactly the three strings `plan_lines_from_groups` already derives per group (`tg.audience_label`, `tg.geo_label`, `group["name"]`) — **`geo_def["kind"]` is never read**, so selection is provably geo-kind-agnostic. Pre-selection never overrides `include_locked`. Disclosure is mandatory and routed, never collapsed: notes naming specific audiences → `unresolved` (client confirms); notes silent, everything selected → `unresolved_internal` (seller check); a `match` phrase that matched nothing → `unresolved`, quoted verbatim; a group skipped because locked → `unresolved_internal`. The stored `draft_plan_intent` (section `"media_plan"` in `DRAFT_KEY_SECTIONS`) keeps the **raw** selection criteria, not resolved ids — `apply_draft_plan_intent_to_new_groups` re-runs the matcher against whatever groups a **later** import produces, which is what makes draft-then-import converge with import-then-draft. **Known, accepted limitation:** a draft run before ANY avails work exists is never even asked about group selection (the prompt section requires real groups to list) and takes the old invent-lines-from-notes path; nothing retroactively reshapes those invented lines once a real import lands later — the normal workflow already pulls avails before drafting, so this is a rep working out of order, not a case worth a rebuild-and-carry mechanism. `apply_draft_plan_intent_to_new_groups` still says so plainly (an `unresolved_internal` note) when it detects the shape. Guards: `tests/test_draft_group_ownership.py`, `tests/test_group_plan_order_invariance.py` (the acceptance test: import→draft and draft→import-where-groups-already-existed converge on identical selected groups/lines/amounts, a redraft re-prices the same group ids rather than regenerating, and the reconciler is idempotent).
- **Whoever replaces state owns all of it.** A staleness guard that only discards stored state on *zero* overlap silently keeps a stale subset when old and new partly agree — which is the commoner case, and which shipped a deck whose totals row said two months while its Flight column said three. `apply_draft_to_form` writes `active_months` outright, and reads the form's own months via `form_flight_months()` where it has no say over the flight. **Partial agreement is the case a staleness guard must be tested against**; zero and total overlap both look correct. Guards: `check_drafted_months_survive_a_used_form`, `check_budget_ties`. → `DECISIONS.md`
- **A keyed widget's session_state entry beats its `value=` argument, so replacing the thing behind it loses data.** Once `option_name_0` exists holding "Option A", re-rendering `st.text_input(..., value="Kickoff", key="option_name_0")` keeps "Option A" — and because the option's name is read back *from* the widget, the drafted name is then written into the option dict and gone. It presented as a cosmetic glitch (only the *first* option was wrong, since option 1's key was new) but it was real data loss. Any structural replacement of the option list — a draft, an add, a remove — calls `bump_plan_options_generation`, and the generation is part of every per-option widget key (`option_name_{gen}_{idx}`), moving the widgets to fresh keys so `value=` is honoured again. Same device as the per-option editor `version`. Applies to any widget whose `value=`/`index=` is computed from replaceable state.
- **The flight's per-month model has one owner, `flight_month_ranges` (FLOW_REWORK_PLAN.md Phase 2)** — the successor to a keyed multiselect that used to ignore its own `default=` once session_state held a value, which is how a drafted flight once produced the label "TBD": the stored months were all invalid against the new range, the selection filtered to nothing. `flight_month_ranges` reconciles a stored per-month list against the current flight bounds with the same rule that incident settled: an empty intersection means the stored choice belongs to a different flight and resets to all-default; a partial overlap is real custom flighting (a skipped month, or one clipped to part of its span) and is kept, clamped into the new bounds rather than dropped. `form_flight_months` and the old inline duplicate in `main()` both collapse onto it now, so there's one reconciliation, not two independently-maintained copies of the same rule. → `DECISIONS.md`
- **The Targeting column carries the whole audience stack, and is per line.** `audience_stack()` joins *every* Campaign Specs Audience bullet, because silent truncation is the worse failure. `resolve_row_defaults` resolves Targeting three ways: the product's own `targeting_copy` when it has one; **the row's own copy, kept**, for an imported broadcast line (its Targeting and especially its Geo come from the Wide Orbit schedule and the station's call sign, and re-seeding the Geo would put a wrong DMA on a client's plan); otherwise the audience stack. All three call sites (`main`, draft, rehydration) must change together, since `default_targeting` feeds `_shared_fields_key`. Geography still uses `first_line()`. Guard: `tests/test_broadcast_e2e.py`, which makes an edit rather than reading a freshly seeded row. → `DECISIONS.md`
- **A plan row's Geo cell prefers the target markets over the Campaign Specs Geography narrative, not the other way around.** `geo_column_default(target_labels, geography_text, originating_label)` used to return Geography text whenever non-empty and only fell back to the joined target-market labels when it was blank — backwards, since Geography is sentence-length narrative prose and a plan cell needs a short market label. Real find (WAEPA, 2026-09-08): a plan row showed the full narrative instead of "Washington, DC, Baltimore." Fixed by swapping the precedence: target labels win whenever they exist; Geography text is the fallback, for the residual case where target-market-name matching itself fails and no labels resolve at all — that fallback is deliberately still narrative text, a known, unfixed edge case. Guard: `tests/test_geo_defaults.py`. → `DECISIONS.md`
- **A proposal carries 1–3 media plan options, and one option must render exactly like no options at all.** `personalize` clones the plan template per extra option and fills each independently, so the plan's scalar tokens are filled per slide, not in the deck-wide pass. The option name is appended to the plan title only when there's more than one. **The plan slide title never echoes the client name twice** — the master deck's own template composes `"{{CLIENT_NAME}} {{PLAN_TITLE}}"` as one run, so a drafted `proposal_title` that already opens with the client name doubled it ("WAEPA WAEPA CTV Strategy"); `option_plan_title` strips a leading, case-insensitive echo (an optional `-`/`:` separator too) before composing, falling back to the untouched title if stripping would empty it. Guard: `tests/test_plan_title_dedup.py`. → `DECISIONS.md`
- **Impressions and Cost are bidirectional, resolved by a per-row driver flag.** `media_plan_driver` records which side the user last typed into and `recompute_row` derives the other. A **CPM edit deliberately does not change the driver**. Drafted rows drive from cost, since the allocation amounts are already group-rounded to tie to the budget. The preview and the deck read `Cost` straight off the row rather than recomputing it — every row on the grid is **net**; the agency gross-up is applied once, at display/deck time, never here (see the gross-up bullet below). → `DECISIONS.md`
- **The agency gross-up is a rep-only, order-wide ×1.15 multiplier on CPM and Cost — never impressions, never anything the model touches** (FLOW_REWORK_PLAN.md Phase 4b, replacing the old `agency_involved` toggle that lived in Section A and was set by drafting). "Apply agency gross-up (×1.15)" sits immediately above the editable plan grid (moved there from "near Generate" — it's the control that changes what a rep types into that grid's CPM/Cost cells, so it belongs beside them) as `agency_gross_up`; `markup = 1.15 if agency_gross_up else 1.0` is derived once, early, and applied in exactly one place, `compute_plan_totals` (via `row_markup`, so the broadcast exemption below still applies). `cost_from_impressions`/`impressions_from_cost`/`recompute_row`/`merge_plan_rows` carry no markup parameter at all any more — a row's stored Cost/CPM are always net. **The editable grid never shows a grossed number — it's net by definition — but the deck-preview table right below it (`preview_display`, built from `compute_plan_totals`'s own `preview_rows`, the same source the deck's own table reads) carries a CPM column that does**, so the toggle's effect is visible somewhere before Generate instead of only inside the generated file. **A number gets grossed exactly once, by the checkbox**: the drafting prompt tells the model to transcribe whatever rate the notes state, verbatim, even when the notes say to gross it up ("the net CPM is $32, gross it up" → the model writes 32, the rep ticks the box, the whole order becomes $36.80) — the settled business rule is that gross-up is always **order-wide**, never per-line, so there is no per-row exemption to build. The avails-PDF importer no longer auto-ticks the checkbox from a document's Agency field either; it surfaces the agency name as an `unresolved_internal` note instead. **Migration for a pre-Phase-4b proposal:** `rehydrate_proposal_into_form` detects one by the *absence* of the new `form_json["agency_gross_up"]` key, and when the legacy `form_json["markup"]` was 1.15, divides every restored non-broadcast, non-flat-fee row's Cost by 1.15 once (impressions are untouched — they were already net either way) and ticks the checkbox, so an old gross proposal doesn't get grossed a second time the instant the box renders. Guard: `tests/test_draft_regression.py`'s standing check that impressions are identical with the gross-up applied and not applied. **`total_budget_basis` ("net"/"gross", `_drafted_budget_basis`) is the same fix `avails_basis` already is, applied to a stated campaign budget**: only an explicit "gross" label converts anything (Python divides by `AGENCY_MARKUP` once, before the waterfall — the genuinely ambiguous case, commission mentioned with no stated side, stays net) and, unlike a bare `unresolved_internal` flag, `apply_draft_to_form` also ticks the checkbox itself — leaving it off after a confirmed gross conversion would show the rep a number nobody asked for. Still the model transcribing a label, never grossing anything itself. Guard: `tests/test_draft_regression.py`'s `check_gross_budget_basis`, `tests/test_draft_live.py`'s phrasing-variant check. → `DECISIONS.md`
- **A media-plan row can be Rate or Flat Fee, and a flat fee is not scaled by month count.** That requirement exposed a real bug: the full-flight total used to be `monthly_total * n_months`, which silently over-counted anything that doesn't scale linearly. Totals now accumulate each row's own full-flight cost and impressions. → `DECISIONS.md`
- **A Monthly-breakout option's Full Flight Total is computed live from `n_months`, never stored per row — editing the setup band's flight after a Monthly-breakout row is priced moves the quoted total with no row ever "rewritten," so no ordinary dirty-row protection catches it.** (A Full-Flight-breakout row's stored Cost IS the full-flight figure directly — immune by construction.) The setup band's flight-change guard warns (naming both dollar totals), blocks Generate, and requires a dismiss click; it does **not** freeze the old total while waiting — see `DECISIONS.md` for why freezing was proposed and rejected. Guard: `tests/test_flight_change_guard.py`. → `DECISIONS.md`
- **Audiences are vertical-independent, and a vertical prioritizes rather than filters.** The avails table and Audience finder are keyed off their own toggle, never off a selected vertical; `prioritize_catalog` sorts, it doesn't remove, and with no hint at all `build_catalog_slice` sends the **full** catalog rather than an arbitrary top-150 cut. → `DECISIONS.md`
- **One custom (non-RFP-selectable) audience per campaign, enforced but never silently.** Checked against the avails table itself whatever the source, with a visible `st.warning` rather than a silent drop. **The draft path caps structurally instead**: `apply_draft_to_form` keeps the first custom segment and moves the rest into `unresolved` as named alternatives, because a prompt instruction is a probability, not a guarantee. Guard: `custom_audience_cap`. → `DECISIONS.md`
- **A $0 media plan row is always wrong** — `resolve_drafted_lines` drops any line resolving to $0 with a note, and the prompt says no-charge items are Included-with-Campaign entries rather than plan lines. A freshly *seeded* rate row is $0 by design; it's fee rows that must never be zero. The one exemption is a `percent_of_avails` line with blank avails, where dropping it would discard the audience. → `DECISIONS.md`
- **% of avails (share of voice) is per LINE, never a deck-wide aggregate.** `show_sov` (default off) gates `_sov_suffix`, which appends `" (X% of avails)"` to a plan line's own impressions — both in the app's preview and in the slide-fill payload's per-row `impressions` field — only when that line has a real match. `matched_avails_for_row` resolves the match through `_group_ids`/`group_ids_of`, the same join merge/split/the map legend already use, never a text match on Targeting/Geo: zero matching ids (a drafted line, a quick-add line, a hand-typed line, a stale id whose group was deleted) is unmatched and shows nothing; two or more ids (a line built by `merge_plan_rows` combining several avails-backed lines into one) sums every id that still resolves, the same combining merge already does for that line's own Impressions and Cost — a line merged from Denver + Atlanta reports its percentage against their combined avails, not against neither market alone or a deck-wide total. `compute_plan_totals` takes an optional `groups_by_id` and stamps each `preview_row` with `matched_avails_monthly`/`matched_avails_full_flight` (the latter scaled by that row's own `row_months`, so a broadcast or merged line's full-flight percentage divides by the flight length that line actually runs in) so the app's preview and the slide read the same number. Blank whenever there's no real, non-zero avails to divide into — never a fabricated 0%/∞%, and never a guess at an unmatched line's avails. Guard: `tests/test_share_of_voice.py`.
- **An avails-PDF import's figures freeze to the DOCUMENT's own dates at import, never the plan's — a rep adjusts explicitly, or not at all.** (FLOW_REWORK_PLAN.md Phase 2, reversing this app's own earlier auto-proration.) `avails_monthly`/`avails_full_flight` are set once, from `avails_daily_rate`/`avails_monthly_from_daily_rate` run with the document's OWN flight on both sides — `avails_full_flight` is the document's stated total verbatim, never round-tripped through rate math. `avails_doc_start`/`avails_doc_end` (ISO strings) and `avails_doc_monthly`/`avails_doc_full_flight` (the frozen figures under their own names) ride alongside so a later adjustment is always reversible. `avails_flight_divergence` names a real mismatch between the plan's active per-month ranges and the document's own — derived by clipping the document's window to each calendar month the SAME way `flight_month_ranges` clips the plan's own, **not** by storing the avails-PDF parser's per-row `AvailsPeriod` list on the group: that was tried, and a raw list-of-dicts value nested inside a `targeting_groups` entry (itself a list of dicts) was found, live against the real 12-group Hershey document, to break Streamlit's own widget-identity bookkeeping on a later rerun — reproducible with no divergence UI, no D2 "Avail dates" column and no plan action involved at all, so this is a storage-shape hazard, not a bug in any of this feature's own code. The derivation is lossless for every document on hand (a real document's periods are always exactly calendar-month-clipped). `apply_pending_avails_plan_adjust` is the reversible, rep-triggered "Adjust to plan dates" / "Use document figure" action, queued the same way every other D2 mutation is; adjusting re-derives from the FROZEN doc figures (never a live, possibly-already-adjusted one, which would compound) reapplied across the plan's own active days (`flight_active_days`, skip-aware). Dates show in the D2 grid (a disabled "Avail dates" column, like "Detached") but never in the generated deck — a second flight competing with the plan's own would raise a question whose answer is internal bookkeeping. Guard: `tests/test_avails_import_proration.py`. → `DECISIONS.md`
- **Co-viewing is an explicit allowlist and a settings record, never a guessed line or a hardcoded number.** `is_coviewing_eligible_row` matches Premion Streaming TV and Live Sports lines ONLY (`COVIEWING_ELIGIBLE_TACTIC_PREFIXES`) — `line_type`/`targeting_copy` can't tell CTV apart from Streaming Retargeting or a sports package, since both carry the same values a retargeting line does. `show_coviewing` (default off) threads a multiplier from `app_settings` (key `coviewing`; `db.fetch_setting`/`load_coviewing_settings`, same cached/fallback shape as the rate card, `FALLBACK_COVIEWING` when Supabase is unreachable) into `compute_plan_totals`, which stamps each eligible line's `coviewing_additional_monthly` — `None`, never 0, for anything ineligible or a flat fee. Shown per line ("+N") in the app preview and on the slide's own `MONTHLY COVIEWING` column (`assembly.add_coviewing_column`, cloned the same way `add_cpm_column` clones Cost, called first so Coviewing lands left of CPM when both are on) — **deliberately no adjusted-CPM column**; the effective CPM (`effective_cpm_with_coviewing`, boosting only eligible lines' impressions before blending) is folded into a short citation instead. That citation lands on the plan slide's EXISTING "Terms & Conditions" text box as a fourth small-print paragraph (`assembly.add_coviewing_footnote`), not a new shape — measured against the real master deck, that box already sits within ~0.2in of the slide's bottom edge, which is exactly why the on-slide text is a condensed multiplier+source+CPM clause rather than the settings record's fuller `footnote` (shown instead in the app, next to the checkbox, where space isn't a constraint). `add_full_flight_total_row` counts optional columns (CPM, Coviewing, either, both, or neither) explicitly from the caller now rather than guessing from cell count, since a single `len(cells) >= 7` threshold could no longer tell them apart once a second optional column existed. Guard: `tests/test_coviewing.py`, including a real-render check that a doubled "Co-viewing:" label (the settings string and the footnote's own bold run both supplying the prefix) was caught only by looking at the rendered slide, not by any string-presence assertion.

### Broadcast and Wide Orbit

- **Broadcast lines are always gross and never marked up — settled, not a default.** A Wide Orbit cost already contains the agency commission, so `BROADCAST_EXCLUDED_FROM_AGENCY_MARKUP` holds the row at markup 1.0 **regardless of the agency gross-up checkbox**, enforced in `row_markup` — the **only** exemption from the order-wide multiplier, structural, never per-line. Recognition is by the **full** tactic suffix (`BROADCAST_TACTIC_MARKER`), deliberately not the shorter `"Broadcast Schedule"`, which is the rate-card product's own label. Renaming the tactic forfeits the exemption, which is what the grid caption warns about. → `DECISIONS.md`
- **An imported schedule replaces the rate-card broadcast line rather than joining it**, and station → market is a lookup, never a guess (`WUSA`→DC, `WPMT`/`FOX43`→Harrisburg; anything else is reported so the seller sets the geo by hand). Impressions and cost come from the WO summary and the **CPM is derived** from them, since the export's own CPM is rounded to cents. → `DECISIONS.md`
- **Wide Orbit schedules parse into one shape, and the units are the trap.** `wideorbit.parse_schedule` reads all three export formats into `Schedule(rows, summary)`. Impressions arrive **in thousands in the Planner exports and raw in the Campaign Schedule Report**, so `normalize_impressions` decides by reconciling against `cost / cpm * 1000` rather than trusting a per-format rule — guessing wrong is a 1000x error in a client-facing plan. Parse failures raise `ScheduleParseError` with a message aimed at a seller. **The test fixtures are real client schedules and are gitignored**; `tests/test_wideorbit.py` reports SKIP without them. → `DECISIONS.md`
- **The schedule's summary block is anchored to the table, not to the template.** `place_summary_below_table` runs after `condense_media_plan_table` and puts it at the table's real bottom plus clearance, clamped so it can't leave the slide; the shape must be captured **before** `fill_bullet_list_in_slide` consumes the token that finds it. Continuation pages have no summary. → `DECISIONS.md`

### Layout, fitting and text measurement

- **Slide content is fitted to the slide, not assumed to fit.** The media plan table's floor is **derived from the slide** (the topmost shape below the table that overlaps it horizontally), never looked up by shape name; font size is derived *from* the final row height, since PowerPoint grows a row whose text doesn't fit; Campaign Specs is measured and scaled to a 60% floor and then warns. Past the point where every row is at the minimum readable height, the table genuinely can't fit and that **warns** rather than shipping an overlap. When testing this, **locate the plan slide by its table** — two slides contain "Included with Campaign". → `DECISIONS.md`
- **The Campaign Specs client name is a width problem, not a height one.** Its box is `wrap="none"` + `spAutoFit`, so a long name draws straight out of the panel. `fit_no_wrap_title` shrinks on one line first (byte-identical to the old single-line behaviour, asserted on shape geometry), and only a name that would otherwise sit at the floor reaches phase 2, which wraps and re-walks the ladder. **The wrapped block grows upward from a fixed bottom**, anchored to where the *template's own* 42pt line ends. Guard: `check_client_name_fit`, which asserts both what the sizer decided and, over COM, what got drawn. → `DECISIONS.md`
- **Text is measured, not estimated, and the font it's measured in has to be the font it's drawn in.** `text_metrics.py` wraps against real glyph advances. Six things are load-bearing and each was a real defect: fonts are **discovered, not listed**; measure at a large reference size and scale down (PIL quantizes to whole pixels); the horizontal inset is not the vertical one; rows are sized **individually**, including headers and rows added after sizing; **a slide resolves its fonts against its OWN master, and this deck has 21** (`prs.slide_masters[0]` is the only one whose minor font is Calibri); and **nothing substitutes silently** — every resolution is recorded `exact`/`substituted`/`unavailable` and surfaced via `measurement_note()`. Guard: `tests/test_font_resolution.py` (offline, machine-independent). **Two settled decisions live with it — font bundling (declined) and `min_table_row_height()` vs PowerPoint's ~0.3in floor (won't fix); don't reopen either without new evidence.** → `DECISIONS.md`
- **A substituted face is measured through a per-face ratio table, not one global constant.** `assembly._SUBSTITUTE_WIDTH_RATIOS` is consulted whenever `resolve_font` reports anything but `exact`; an unmeasured face gets the worst ratio observed on any face, derived as `max()` of the table rather than typed in. Three paths need three different corrections (measured in the face / measured in Calibri / no font files at all), and a run whose family is present but whose bold cut is missing gets 1.0 and is not reported. Guards: `check_width_calibration` (offline), `check_client_name_fit` (COM). **Don't re-litigate extracting the embedded fonts** — they are EOT `TTCOMPRESSED` and PIL cannot open them. → `DECISIONS.md`
- **A cramped plan buys its type back from the slide, not from the reader.** Two font floors, and conflating them is what left long plans at 6pt: `_MIN_TABLE_FONT_PT` (6) is the absolute floor; `_COMFORTABLE_TABLE_FONT_PT` (9) is the signal to spend layout elsewhere. The threshold is a **font size, never a row count**. What it spends is the "Included with Campaign" band — but the plan slide is a **signable document**, so the band may be made smaller and moved and may **never leave the page the signature is on**. Sizing runs twice (carry `template_metrics` across both passes), the band is parked low during compression and floated back afterwards, and the decision is **per deck, not per slide**. → `DECISIONS.md`
- **Reserving generously and refusing generously are different things.** `_row_height_for` keeps a 0.1in per-row cushion because PowerPoint draws these rows taller than their content accounts for; the "cannot fit" warning is judged on `_row_text_height` (bare lines, no cushion), because judging it on reserved height declared a 12-line plan too tall for a slide PowerPoint then drew with room to spare. → `DECISIONS.md`
- **Validate layout against the renderer, and read PowerPoint's row heights correctly.** PowerPoint reports a row's height as **max(declared, needed)**, so a row reported *at* its declared height says nothing — the real signal is a row reported **taller** than declared, meaning the text didn't fit and PowerPoint grew it. Pair tables by **signature** (slide, shape, first row), never by index, and scope the check to tables the sizer manages. `tests/validate_text_metrics.py`. → `DECISIONS.md`

### Drafting, the catalog and the Claude API

- **The AI draft path does no arithmetic — Claude returns intent, Python does the math.** Claude returns `media_plan_lines`, one entry per intended **row**; each line's `allocation` is exactly one of `flat_amount`, `percent_of_total`, `percent_of_remainder`, `split_evenly` or `percent_of_avails`, resolved as a waterfall (avails and fixed amounts first, then remainder, then even split) before impressions are computed from the same `PRODUCTS` CPM table the rest of the app uses. A line can name `custom_fee` for a one-time fee, or `sport:<key>` for a package. Segments Claude names are validated against the real catalog and dropped with a note if unmatched, never silently kept. → `DECISIONS.md`
- **A drafted plan is authoritative over Section C.** The draft clears every product toggle before switching on only the ones its lines use, and the prompt states outright that there is no mandatory line and no default product. Sports selection is the union of the `sports` array and any `sport:` lines. → `DECISIONS.md`
- **The seed key is built by `read_seed_selections` and nowhere else.** A hand-rolled copy that merely omits a key makes `main()` take its "product selections changed" branch and reseed every option's rows to $0 while leaving the option *names* alone — which presents as "the budget allocation is broken" and is near-undiagnosable. `read_products_selection(get=None)`/`read_seed_selections(get=None)` take an optional getter so there is one construction. **Never rebuild that dict at a call site**; adding a product widget to `read_products_selection` is the whole change. → `DECISIONS.md`
- **A drafted line may carry its own `cpm`, and an override always announces itself** in `unresolved`, because nothing downstream distinguishes a negotiated rate from a standard one. Omit the field to take the rate card default; a non-numeric or ≤ 0 value falls back rather than pricing at zero; a `cpm` on a `custom_fee` line is ignored and flagged. `unresolved` is deduped on the way out. → `DECISIONS.md`
- **A drafted budget is always a campaign total; the breakout is a separate, presentational question.** `resolve_drafted_lines` works in full-flight amounts and `spread_rows_over_months` divides rate rows across the flight (deliberately unrounded, so month × count still ties to the quoted budget). Flat fees are never divided. **Every option must carry its own `total_budget`**, and an option that can't be priced is named in the error rather than reported as "no usable lines". → `DECISIONS.md`
- **A re-draft is merged over the previous draft**, because a dropped field isn't neutral — the incident that settled this: a re-draft once dropped the (now-retired) `agency_involved` field, and a missing boolean read as false rather than "unchanged," quietly repricing every line at net. `call_claude_redraft` returns `{**previous, **revised}`. One clarification round only (`draft_round`), with hand edits protected at section granularity via `DRAFT_KEY_SECTIONS`, and preserved sections named back to the user. → `DECISIONS.md`
- **Avails are ingested when the notes supply them, invented never, and always stored monthly.** `audiences[].max_avails`/`avails_basis` are populated only from a figure the notes state — never estimated, carried across, or worked back from a budget. The D2 toggle changes what is *shown* (and the slide's column header, which is literal text rewritten run-by-run); `restore_untouched_avails` keeps an unedited row byte-for-byte so flipping the basis can't walk a stored number. Reach is invariant to the basis. Guard: `tests/test_avails_reach.py`. → `DECISIONS.md`
- **The drafting prompt states rules positively — a rule that quotes what it forbids can teach the exact output it is trying to prevent.** A live run echoed the very identifier the prohibition quoted. Say what to write, not what not to write; where a field name must stay in context, put the plain-language substitute at the point where the field is explained. → `DECISIONS.md`
- **A failed Claude call has to say which failure it was — to the log, not to the rep.** `stop_reason` is read **before** the JSON is parsed (a truncated response is not a formatting problem), text is joined from every block, `ANTHROPIC_MAX_TOKENS` is **16000** — sized against the biggest realistic draft and the practical ceiling for a non-streaming call — and every call is logged (label, stop reason, tokens, request id, first/last 200 chars) to the console and to `%TEMP%/proposal_builder_claude_calls.log`. **A preamble instead of raw JSON is salvaged, not prevented**: `_extract_largest_json_object` scans every balanced `{...}` in the response and takes the **largest**, not the first — the first was tried and shipped, and it silently corrupted a real draft the very next day, because a preamble routinely quotes a small schema fragment verbatim while reasoning (`{"mode": "all"}`) and that fragment is itself complete, valid JSON. An assistant-turn prefill was tried as the structural fix and is a dead end on this model — `claude-sonnet-4-6` 400s on it ("does not support assistant message prefill") — don't re-attempt it without re-checking that against the model in force. Empty, truncated and unparseable responses retry once, appending a corrective instruction to the prompt rather than resending an identical one; a refusal does not retry. **Every message a rep actually sees is plain language — what happened, then retry, then "Report an issue" — with no file paths, session keys, or function/constant names.** The real diagnostic detail (stop_reason, first 200 chars) goes into `st.session_state["last_claude_failure"]` instead, scoped to the single most recent failure and cleared on the next success, which `capture_feedback_state` folds into a rep's own bug report. Guard: `tests/test_claude_call.py`. → `DECISIONS.md`
- **Vertical detection matches whole words over one combined table, longest phrase first.** `VERTICAL_HINT_SYNONYMS` carries ~136 trade names; matching is **word-boundary regex, not substring** ("auto" in "automatic", "spa" in "Spanish"), and trade names and vertical names live in **one** table so "credit union promoting auto loans" doesn't resolve to `auto`. Terms describing what's advertised rather than who's advertising don't belong here. Guard: `test_vertical_hints.py`. → `DECISIONS.md`
- **Dynamic Video Ads are a flat fee.** The Section C toggle includes the slide and seeds a flat-fee row; the product sits in `PRODUCT_TO_WIDGET_KEYS` and `ATTRIBUTION_FIELD_MAP` but is excluded from the line namespace via `NON_LINE_PRODUCT_KEYS`, or a drafted line could name it, find no CPM and produce a $0 row. → `DECISIONS.md`

### Case studies

- **A case study goes into a deck as images when it has them, and as copied slides when it doesn't.** `append_case_studies` takes either shape and `app.case_study_source` resolves both, so generate and rebuild can't disagree. Images exist because cross-deck OOXML copying produced five distinct corruption bugs and still can't reproduce PowerPoint's live autofit; they are also **smaller**. The permanent cost is that an image slide isn't selectable or editable text, which is why the copy path stays. → `DECISIONS.md`
- **Cross-deck copying is much harder than in-deck duplication.** `copy_slide_into` imports every referenced part and renumbers it: **never `get_or_add_image_part`** (it re-encodes and fails on EMF/SVG), **a chart is not a leaf** (it carries a workbook plus colour and style parts, and rIds inside copied XML need rewriting), and **slide backgrounds live on `<p:cSld>`**, outside the shape tree. One `ImportCache` must span a run of copies. → `DECISIONS.md`
- **A copied case study slide inherits nothing, because it is flattened on arrival.** `slide_inheritance.flatten_inherited_formatting` resolves placeholder geometry, body/run/paragraph properties, then scheme colours and theme fonts **last**, strips `<p:ph>`, adds explicit `prstGeom` (a de-placeholdered `<p:sp>` *or* `<p:pic>` has no geometry and draws nothing), and copies the layout's and master's own shapes behind the slide with `showMasterSp="0"`. **The faithful alternative — importing the source layout/master/theme — was built and abandoned**; don't retry it without new information. One residual is reported rather than hidden, counted by `tests/test_case_study_import.py`. → `DECISIONS.md`
- **Case study images are generated locally, never by the app.** `render_case_study_images.py --pending|--all|--list` renders over PowerPoint COM and uploads to the `case_studies` bucket; Streamlit Cloud has neither PowerPoint nor Proxima Nova, so rendering there would store images that aren't what the case study looks like. Empty `slide_images` *is* the pending queue. `render_pending.bat` and `schedule_render_pending.ps1 -Register` run it hands-off; `--skip-if-busy` exists because a COM `Dispatch` **attaches to a running instance**. → `DECISIONS.md`
- **Never key a cache on `id()` when the objects can be collected.** `copy_slide_into` opens its source `Presentation` locally, so its parts are garbage the moment the call returns and CPython reuses the addresses — a style part landed on a freed workbook's address and the cache handed back the workbook. Keyed on `(id(part.package), partname)`, with the source presentations retained. Deduplication is limited to `/ppt/media/`: two charts sharing one embedded workbook is a file PowerPoint refuses. **Both failures were invisible to every structural check** — which is why a failure to open counts as a test failure, and why `package_check.check_package` runs on every deck the fast suite builds. → `DECISIONS.md`

### Slide vault

- **The case-study vault again, one slide at a time — a colleague uploads a `.pptx` and ticks the individual slides worth keeping, never pre-selected into a proposal.** One row is one picked slide, not one upload — several `slide_vault` rows share one `storage_path` (`slide_index` tells them apart), so a deck is never split apart the way extracting a single slide out on its own would require; that's exactly the cross-deck copy problem the case-study vault's five corruption bugs came from. Verticals/products/a free-text `purpose` tag it; `db.slide_vault_render_path` (`"image"`/`"copy"`) is the image-vs-copy signal, `slide_image` singular where `case_studies.slide_images` is an array, null meaning not-yet-rendered — the pending queue, same convention. `render_slide_vault_images.py` groups pending rows by `storage_path` so a source deck is opened through PowerPoint COM once regardless of how many slides were picked from it.
- **Placement is per slide, resolved fresh after every graft, never cached across one.** `assembly.vault_slide_insert_index(prs, placement)` derives `"front"` (index 1, after the cover), `"before_plan"` (the same `case_study_insert_index` call case studies use, so a vault slide grafted after them lands after them too, not interleaved or ahead) and `"appendix"` (current end of deck) by scanning the deck AS IT STANDS. `append_vault_slides` groups selected slides by placement and re-derives each group's anchor immediately before grafting that group — computing all three up front, before any group is inserted, is the exact bug `tests/test_slide_vault.py`'s combined-order check exists to catch (a stale position lands a `"before_plan"` slide in the middle of the case-study block instead of after it). Must run after `append_case_studies` and, like it, before `personalize()`. `app.vault_slide_source(row, placement)` takes the rep's chosen placement as an argument rather than reading the vault row's own stored default — reading the row would drift a rebuild the moment someone edited the vault entry afterward.
- **Schema is live** (`supabase_schema.sql` Stage 12, pushed via `setup_supabase.py slide_vault`) — confirmed 2026-09-08 querying real rows. → `DEPLOYMENT.md`
- **A vault entry can be hard-deleted, not just deactivated — for a test upload or a wrong file that shouldn't sit in a shared library at all.** `db.delete_slide_vault_entry` refuses, naming which proposals, if any logged proposal's `form_json["vault_slides"]` still references the id — unlike case studies, which have **no delete at all, only deactivate**, a hard-deleted row would break "Rebuild as presented" for anything that used it, since `fetch_slide_vault_entry` needs the row to still exist regardless of `active`. The underlying storage file is removed only when no sibling row still shares its `storage_path`. UI reuses proposal history's own `delete_proposal` popover-plus-typed-"DELETE" shape — the only hard delete this app actually had to reuse. Guard: `tests/test_slide_vault_delete.py`.

### Streamlit, session state and the app shell

- **The Build form outlives page navigation, because Streamlit garbage-collects the session_state entry of any keyed widget not rendered during a run.** Measured: 111 widget keys gone on a page switch, 14 non-widget keys untouched. `snapshot_form_state()` mirrors the form into an ordinary key after every widget is instantiated and before Generate does anything; `restore_form_state()` puts back **only keys that are ABSENT**, which is what makes it safe beside the draft and rehydration paths. Three classes of key must never enter the snapshot — unsettable widgets (Streamlit *raises*), `data_editor` deltas (which are edits, not data), and navigation — plus another page's widgets. The logo keeps its bytes in `uploaded_logo` because an uploader can't be restored. **"New proposal" clears by deny-list.** Guard: `tests/test_form_state.py`, which walks the AST rather than trusting a hand-written list. → `DECISIONS.md`
- **A docstring that isn't the first statement is not a docstring — it's copy on the page.** Streamlit's magic rewrites a bare string expression into `st.write()`, and several paragraphs of internal rationale rendered on the login screen for every user. Rationale that sits below any code goes in `#` comments. Guards: `tests/test_no_stray_magic.py` (AST, Streamlit's own rule) and `tests/test_entry_screens.py` (asserts the whole inventory of visible copy). → `DECISIONS.md`
- **Streamlit dev server caveat:** `import assembly` / `import slide_map` are cached in the running process's `sys.modules`. Streamlit's file-watcher reloads and reruns `app.py` on save, but does **not** reload already-imported modules — editing `assembly.py`/`slide_map.py` while the server is running will silently keep executing the old code until the `streamlit run` process itself is restarted. Always restart the process (not just the browser tab) after editing those files, and treat a test that "should have changed but didn't" as a signal to check for this first.
- **The same staleness happens on Streamlit Cloud, and there "pushed" is not the same as "running that code".** A redeploy can reuse a warm process, which took the live app down with an `AttributeError` on a name whose two halves had always shipped in the same commit. **Triage an `AttributeError` on a cross-module name by rebooting first, before touching git** — the tell is that the failing combination exists in no commit (`git log --oneline -S "<name>"` on each file settles it in seconds). Reverting is the tempting move and the wrong one. `tests/test_cross_module_refs.py` covers the *other* failure, the genuinely half-complete push. → `DEPLOYMENT.md`
- **When a feature is verified working in tests but reported broken in the running app, restart the app before investigating anything else.** Happened twice now: a market-lookup registration and a targeting-map slide placement, both landed in real commits, both passing every offline test and a fresh `AppTest` process with zero pre-installed state, both reported dead in the live session anyway — a warm process that predates the feature, same root cause as the `AttributeError` case above, just presenting as "silently doesn't work" instead of a crash. Don't spend the investigation re-deriving that the code is correct; restart first (`python tests/app_process.py restart`, see below), reproduce again, and only dig into the code if it's still broken after that.
- **Any change to the Streamlit UI or a generated slide comes back as a screenshot of the affected area at normal browser width, not a written description** — the existing "look at the pictures" rule (below, under Testing) covered rendered slides; this extends it to the app's own UI. A summary describes intent, and intent has been correct in every case where the output was still wrong: twelve stacked "Apply to all" buttons, twelve duplicate geography expanders, an intake column printing its own label twice, a targeting map silently omitting two-thirds of a document's impressions — all invisible in prose, obvious in an image. **Before sending the screenshot, run a rep-lens pass and report the answers, not "checked"** — the user is a working sales rep building a client-facing proposal, not a developer: does every visible line require the rep to act (if not, it's internal, not UI); what does this look like with 12 rows instead of 2 (does anything repeat per row that should appear once); is any label, word or control rendered twice; does the output account for every input row, stated both ways (on-map/exported count vs. total); what's the first thing a client would ask about this. → `DECISIONS.md`
- **The real Hershey & Harrisburg avail (RFPID-260402) is the standard fixture for anything touching avails import, the targeting map, or the D2 grid** — a synthetic 2-audience/3-market case passes designs the real document breaks (a 12-row document with two audiences sharing identical geography across every row was never imagined by any synthetic fixture, and made the map's whole color-by-audience legend meaningless until it was found from real use). `tests/group_scenario_fixtures.py` also carries Annapolis Cars (RFPID-253813, nested-radius geography) and Wilmington University (RFPID-253956, County Option zip-originated geography) for the same reason.

### Identity, proposal history and the finders

- **Identity is attribution, not authentication — and it never blocks.** The shared `APP_PASSWORD` is the only gate; `check_identity` asks who's using the app so a proposal, upload or attached file can say where it came from. **If Supabase is unreachable the step is skipped entirely.** The list builds itself (anyone can add a name), dedupes on a stored `name_key` while keeping the display name as typed, and `created_by` is **nullable on purpose** so pre-identity rows stay anonymous rather than attributed to a guess. → `DECISIONS.md`
- **Usage stats are read-only and computed from what's already logged** — no events table. Every count has to survive an empty database. → `DECISIONS.md`
- **In-app feedback (BACKLOG.md) — core built 2026-08-23.** A "🚩 Report an issue" `st.sidebar.popover`, rendered on every page (`render_feedback_popover`, called from `main()` before the page dispatch) — category, free-text notes, no screenshot yet (deliberately deferred; state capture alone covers most of the reproduction value). `capture_feedback_state` grabs a curated, best-effort subset of session_state — `targeting_groups`, `plan_options`, the scalar Section A/flight fields, drafted notes, the review lists — **deliberately never a `data_editor`'s own widget state** (this file's documented delta-vs-data trap) and never Generate's derived, markup-applied numbers, which aren't reconstructable from outside the Generate handler anyway. `feedback` is a new Supabase table (`state_json` jsonb, a few KB — no storage bucket needed, unlike a screenshot would); `db.submit_feedback`/`fetch_feedback`/`update_feedback_status`/`count_open_feedback` follow the same fallback/tuple convention as everything else in `db.py`. The submit button's key (`feedback_submit_{gen}`) is in `NON_PERSISTABLE_PREFIXES` — it renders on the Build page too, so without that exclusion `restore_form_state` would try to write into an unsettable button and raise. `render_feedback_admin_page` ("Feedback reports" in the nav) lists newest-first, filters by status/category, marks open/closed, and exports selected reports as one markdown bug report. Guard: `tests/test_feedback.py`, entirely stubbed against a fake in-memory store (the real table doesn't exist in Supabase until the migration in `supabase_schema.sql` is pasted in by hand, and a test must never write real rows into production either way — same reason `test_draft_regression.py` stubs `db.log_proposal`). **Deferred, not forgotten:** a screenshot attachment, and loading a report directly into the form (the captured state is a curated subset, not the full `form_json` a rebuild needs) — see BACKLOG.md's carry-over section.
- **Proposal history is append-only, and reuse never rewrites what a client was sent.** Regenerating a loaded proposal **inserts a new row** carrying `parent_proposal_id` (`ON DELETE SET NULL`, never cascade). **Rehydration goes through `read_seed_selections` like everything else**, writes every widget key before that widget renders, and marks restored rows dirty. **"Rebuild as presented" uses the stored `deck_payload` verbatim and the proposal's own deck version** — recomputing would re-price against today's rate card — so `fetch_deck_version`/`fetch_case_study` deliberately ignore `active`. The client logo is stored, and three states are kept distinct: no logo, logo stored, and had-a-logo-that-can't-be-fetched (rebuild anyway, but warn). `revision_label` annotates the row, never the deck. An **attached final file** is the one exception to storing-the-recipe, and makes "Rebuild as presented" warn. **"As presented" is now a byte-identical claim, verified, not just an intention** — `tests/test_group_backward_compat.py` generates a proposal, then builds it back two independent ways (Rebuild as presented; Load into form + Generate untouched) and diffs the two `.pptx` files part-by-part, excluding only the ZIP container's own save-time stamp. It caught a real one: an empty Timing narrative fell back to `"--"` in `rebuild_proposal_deck` but to the flight label in the live Generate handler — two independently-drifted constructions of the same fallback, fixed by having rebuild use the flight label stored in `form_json` (never recomputed) like everything else about a rebuild. → `DECISIONS.md`
- **Both finders have two entry points and one implementation.** `_audience_finder_body` is shared; **`avails_df is None` means standalone** (no "Add" buttons). The vault browser edits tags inline rather than requiring a re-upload, and per-case-study downloads are deliberately not eager. → `DECISIONS.md`

## Running things

- **Assembly engine standalone:** `python assembly.py` — runs the hardcoded `SELECTIONS`/`FILL_DATA` at the bottom of the file against `TEGNA_MASTER_DECK_v1_1.pptx` and writes `test.pptx`.
- **Full app:** `streamlit run app.py`, then log in with the password in `.streamlit/secrets.toml`. Pass `--server.headless=true` to skip Streamlit's first-run "enter your email" prompt, which otherwise blocks on stdin.
- **Local test mode: `PROPOSAL_BUILDER_TEST_MODE=1` skips the password gate and preselects a test user**, so the real UI can be driven without sharing a password. Read **only** from the OS environment (never `st.secrets`), refused when the process looks like Streamlit Cloud, and loud on every run. The same flag gates fixture injection for uploads, since `st.file_uploader` can't be driven by AppTest. → `DECISIONS.md`
- **Look at the pictures.** `python tests/render_scenarios.py` renders the standard scenarios to PNG and saves the decks beside them (under `deck_render.render_root()`, printed on every run, outside the repo). Every layout failure this project has shipped was invisible to an assertion and obvious in a rendering. It uses **PowerPoint through COM**, not LibreOffice; Windows-only, every caller skips when it's absent. → `DECISIONS.md`
- **Drafting regression suite, in two tiers.** Run tier 1 before any push touching drafting, assembly, or the rate card; run tier 2 as well before pushing a change to the prompt, the draft schema, the audience catalog, or the model version.
  - **Tier 1 — `python tests/test_draft_regression.py`** (fixture-name prefix to run one scenario, `--keep` to leave the decks on disk). Free, offline, ~1 minute. A recorded model response goes through the real `apply_draft_to_form`, the real form via `AppTest`, and the real assembly; the spies call through, so assertions see exactly what the app built. It stubs `db.log_proposal` (it used to write real rows into production). Treat a committed `.draft.json` as **frozen**. → `DECISIONS.md`
  - **Tier 2 — `python tests/test_draft_live.py`** (`--save` writes raw responses to gitignored fixtures). Live API calls, a few cents — the assistant may run this directly under the standing authorization below, or the user can via `!`. Asserts **only model-layer behaviour**, structurally. A failure means the model drifted from what the prompt asks; re-run once before concluding anything. → `DECISIONS.md`
- **`python tests/run_all.py` runs literally every `tests/test_*.py` file, not a curated subset — a standing gate, run before starting a phase and before declaring one done, on top of (never instead of) Tier 1/Tier 2.** A hand-picked "suites I thought were relevant" list has already missed a real regression this project shipped: a new D2 column broke three OTHER suites' `st.data_editor` monkeypatches, each in a file nobody thought to re-run because none of them looked related to the change. Tier 1 stays a separate, narrower command on purpose — its whole value is being free, offline and ~1 minute (though "1 minute" is optimistic on a loaded machine — 4m27s was measured live building this), cheap enough for every push touching drafting/assembly/the rate card; the full sweep is neither fast nor uniformly runnable (several suites `SKIP` without gitignored real client PDFs) and is a coarser, less frequent gate instead. **`test_draft_live.py` (Tier 2) is excluded by default** (`--include-live` overrides it) — a routine sweep must never spend real Anthropic API money or fail the gate on probabilistic model drift that isn't a code regression; Tier 2 stays its own explicit, occasional command. Each file runs in its own subprocess (never imported into one shared process, which has separately been shown to let one suite's state bleed into another's — see DECISIONS.md's AppTest nested-data finding), reports PASS/FAIL/SKIP/TIMEOUT, and exits non-zero only on a real failure. `python tests/run_all.py <substring>` narrows to matching filenames while iterating; the no-argument form is what satisfies the standing-step requirement. **A default sweep splits itself into 8 chunks, each run by its own fresh `run_all.py --chunk i/8` subprocess, and that is the standard invocation — an unchunked run (`--chunks 1`) measures ~4x slower for machine-level reasons this project has deliberately not chased.** The gate is ~20 minutes chunked, ~78 unchunked, on identical code with identical results; the split was measured (a file is 47s standalone, 48.7s as the only file under the harness, 45.6s in a 10-file chunk, 181.9s in one 78-file run) and the slowdown is uniform across every file, hits trivial files as hard as heavy ones, and doesn't grow with position — so it is neither harness overhead nor accumulation across children. Don't re-derive that; just keep the chunking. `DEFAULT_CHUNKS` in `run_all.py` carries the same note. **First real run already found a genuine, pre-existing defect**, unrelated to whatever prompted the run: `test_targeting_map.py` hung/ran pathologically slowly, inconsistently across attempts (progress on one run varied wildly from another under otherwise-identical conditions) — confirmed as real CPU-bound work, not a deadlock, by watching its process's own CPU time climb, and reproduced enough times to rule out a fluke. **Root-caused and fixed 2026-09-03**: `targeting_map._projector`'s span floor was a literal `1e-6` degrees rather than a real-world minimum, so a near-point group (one zip is enough) drove the projection scale to ~9×10⁸ px/degree and flung nearby county/state boundaries to billion-pixel line coordinates — Pillow's line rasterizer took 6-10s per call at that scale. Floored at the same real minimum `_matching_boundaries`'s search box already uses (`_MIN_SEARCH_DEGREES`) instead. The file now runs in ~25s. → `DECISIONS.md`

  **When a fix is "apply this same change to every file matching pattern X," find those files by grep, not by recollection, and record the grep in the commit message.** Two separate curated-list misses in one day (2026-08-28) made this a standing rule rather than a one-off lesson: commit 4 of FLOW_REWORK_PLAN.md Phase 3 listed the specific test files to re-run for its D2 column rename and missed one (`test_avails_import_proration.py`'s own `fake_data_editor` picked the avails column via the same stale-exclusion-tuple pattern three OTHER files already had fixed for the same rename); separately, `ff0d2e5` ("Fix Phase 1 gate regressions across ~20 AppTest suites") fixed every file it found broken by the setup-band gate except `test_targeting_map.py`, which stayed silently broken (Generate never rendered) until this same day's full sweep caught it. Both were exactly the shape `run_all.py`'s own no-argument sweep exists to catch, and both survived one full-sweep run already (2026-08-27) before this one caught them — a full sweep only helps if something actually runs the affected file; a list assembled from memory of "which files touch this" is the failure mode, not the length of the list. The fix, going forward: when a change requires updating N call sites of the same shape, generate the list with `grep`/`Grep` against the pattern that actually defines membership (a column-name literal, a session_state key, an import), not from remembering which files seemed related — and paste the grep into the commit message so the next person can re-run it and confirm nothing new matches.

  **Same family, and the one that keeps catching this project out: before moving, deleting, retiming, characterising, or reasoning about a test or a fixture, READ THE FILE — not the doc that describes it.** Docs describe intent; the file is what runs, and the two drift. Three separate instances in one day (2026-09-06/07), each of which produced a confidently-stated wrong answer that a single `Read` would have prevented: (1) `Premion OTT.xlsx` was called "Mattress Warehouse's delivery export" throughout `ATTRIBUTION_REPORT_PLAN.md` — parsing it shows Cardinal Plumbing on every campaign-name row, RFPID 256286, matching Cardinal's own attribution file; (2) "there is no real MW delivery export in the repo" was asserted after searching for the file that was *expected* rather than listing what was actually there — `MW delivery.xlsx` was sitting in the project root the whole time, and it carries the exact 3.1M figure a test had just been written synthetically to stand in for; (3) `test_group_geo_resolution.py` was described as "535s of live Census calls with wild variance" and nearly moved out of the gate on that basis — reading it shows **one** network call, to a deliberately bogus address, costing 0.22s, with every other center a bare zip that `geo_resolver.resolve_center` short-circuits to the local crosswalk. That characterisation came from CLAUDE.md's own prose about the geo *feature* plus a variance note about a *different* file (`test_targeting_map.py`, since root-caused and fixed). **A file's identity, cost, and behaviour are properties of the file. Establish each from the file itself — open the workbook and read the client/RFPID out of its cells, `ls` the directory rather than guessing the filename, read the test body rather than the doc paragraph about the feature it tests — before acting on any of them.** The tell that this rule is being broken: a claim about a file is being made and the file has not appeared in a tool result this session.
- **A check whose expectation comes from the same source as the code under test proves only that the code agrees with itself.** Four instances so far, all silent until someone read the output carefully. **Assert against something the code can't move**: the budget the notes stated, Wide Orbit's own totals row, the rendered slide, `active_months` as the form holds it, what the seller sees on screen. **Corollary:** when a value may legitimately live in more than one place, the failure output must print **all** of them, or a reproducible failure is indistinguishable from model drift. → `DECISIONS.md`
- **Headless testing:** the bottom-of-file `main()` call is guarded by `if __name__ == "__main__":`. Streamlit execs the entrypoint script as `"__main__"`, so the app is unaffected, but the guard lets a test `import app` for its pure helpers (`recompute_row`, `resolve_drafted_lines`, `compute_plan_totals`, `line_product_spec`, …) without rendering the form. For anything that needs a real `session_state`, `streamlit.testing.v1.AppTest` runs the actual script headlessly — set `at.session_state["authed"] = True` to get past the password gate, then drive widgets and assert on `at.exception` / `at.session_state`. Functions that only touch `st.session_state` (e.g. `apply_draft_to_form`) can also be tested by temporarily swapping `app.st` for a stub holding a plain dict.
- **Standing authorization: the assistant may make live Anthropic API calls itself when diagnosing or verifying drafting behaviour** — driving the running app's draft/redraft/audience-suggest calls, or running tier 2 directly — without asking first each time. Real money, so still be the cheap version of thorough: read `%TEMP%/proposal_builder_claude_calls.log` (or ask for the Streamlit Cloud log lines) before re-running anything, and reproduce with the smallest notes/scenario that shows the behavior rather than the user's full real notes. `.streamlit/secrets.toml` itself stays fenced off (can't be read or checked for existence by any tool) — the running app reads `ANTHROPIC_API_KEY` from it at runtime regardless.
- **One thing the assistant can't test directly:** the master deck can't be opened while it's open in PowerPoint (python-pptx surfaces the Windows file lock as a bare `PackageNotFoundError`, whose real cause is a `PermissionError` on `open()` — check that first before suspecting the path).
- Python and `streamlit`/`pandas`/`python-pptx`/`Pillow`/`pdfplumber`/`anthropic` are installed locally (there was no system Python before this project — installed via winget). `git push` needs an interactive login (Git Credential Manager) that the sandboxed shell can't do; the user runs it via `! git push` themselves.
- **The project lives at `C:\projects\proposal-builder`, deliberately outside OneDrive, and nowhere else.** Sync held handles on directories mid-`rmtree`, held freshly written decks so PowerPoint refused them, and raised **modal** conflict dialogs that block every COM call. Renders go to `deck_render.render_root()` under `%LOCALAPPDATA%\Temp`, outside sync wherever the project sits. → `HISTORY.md`. A OneDrive clone of this repo existed at `C:\Users\mfreedman\OneDrive - TEGNA\Documents\Proposal Builder` from before the move and was deleted 2026-08-18 after confirming its git history was fully contained here and every untracked file it held (secrets, master deck, case study source, Wide Orbit fixtures, avails PDFs) was already identical in this directory. **If a session finds itself running from a different directory than `C:\projects\proposal-builder`, stop and check before doing anything** — don't assume it's an equivalent copy. And when the CLAUDE.md size-limit warning fires, check which directory the session was launched from *before* editing this file — a stale copy elsewhere is the likelier cause, not this file actually growing.
- **Commit or stash before any destructive git operation, and before deliberately breaking something to test it.** `git checkout <file>`, `git reset` and `git stash` all discard uncommitted work, and this has now thrown away a mid-task fix twice. The trap isn't carelessness about the command — it's that the *reason* for running it looks unrelated to the work in progress: reverting a defect that was reintroduced on purpose to prove a test catches it, or undoing a bad edit, silently takes every other uncommitted change in that file with it. Both times the file also held a real fix that had been written but not yet committed. So: commit the working state first (or stash it), then break things. Verifying that a guard fails when the defect returns is a genuinely valuable check and worth doing — it's what proved `test_no_stray_magic` and `test_entry_screens` actually work — but the reintroduce-and-revert cycle has to start from a clean tree, or the revert is indistinguishable from losing the fix. Check `git status` before and after; an unexpectedly clean tree after a revert means work was destroyed, not saved.
- **Never force-kill a GUI application — ask first.** `taskkill` on `POWERPNT`, `EXCEL`, `WINWORD` and friends looks like clearing a stuck automation instance and isn't: the same process may be a window the user is working in, with unsaved changes, and killing it destroys them silently. This happened here — a `taskkill /F /IM POWERPNT.EXE` was run to clear what turned out to be PowerPoint waiting on a modal dialog the user hadn't seen yet. Close through COM instead (`Presentation.Close`, `Application.Quit`), and if that fails, **say so and stop**: "PowerPoint appears to be showing a dialog, can you check?" is the right move, because a modal dialog blocks every subsequent COM call ("Cannot perform this action with a modal dialog displayed") and only a human can dismiss it. Related: renders are written outside the synced folder precisely so OneDrive stops raising those dialogs — see `deck_render.render_root`.
- **Permissions:** `.claude/settings.json` (committed) allowlists routine commands so they don't prompt every time — git status/diff/log/add/commit/push/pull/branch/checkout, python/python3/py, `streamlit run`, pip install/list/show, read-only file inspection (ls/cat/head/tail/etc.), and Edit/Write within the project. It explicitly denies force-push and history-rewriting git commands, recursive deletes, and any read of `.streamlit/secrets.toml`. Personal one-off approvals go in `.claude/settings.local.json` instead (gitignored, not shared).

## Status

**Live and working.** Five pages (Build a proposal / Audience finder / Case study
finder / Add case study / Update master deck), plus proposal history, usage
stats and identity. Deployed at https://proposal-builder.streamlit.app/.

- **Master deck:** the active version is **5** — the key-tagged v1.1 deck, 120 slides, all resolving from notes labels. Older versions stay in storage so "Rebuild as presented" can reproduce an older proposal. **Don't hardcode the version or the slide count anywhere** — read them from `deck_versions` and the deck; both have drifted in this file already.
- **Supabase:** `deck_versions`, `products` (10 media products + 21 Live Sports packages), `audiences` (369 brochure segments, 105 RFP-selectable, plus whatever an activated usage workbook has merged in since) + `audience_usage`, `audience_usage_versions`, `proposals`, `case_studies`, `team_members`, `app_settings` (one row today, `key='coviewing'`); private `decks`, `case_studies`, `proposal_files` and `audience_usage_workbooks` buckets. Verified live, including the fallback path with the URL pointed at a nonexistent project. Audience-usage versioning/merge is live too — version 1 (`Premion OTT Audiences 7.1.26.xlsx`, uploaded 2026-08-21) is active in production, confirmed by querying `audience_usage_versions` directly. Run `setup_supabase.py settings` before the co-viewing checkbox has a real citation to show — until then it runs on `FALLBACK_COVIEWING` with a visible warning, same as an unreachable rate card.
- **Case study vault:** 22 case studies (home_improvement 4, auto 3, retail 3, banking 2, education 2, entertainment 2, healthcare 2, dining_qsr 1, travel 1, plus three law-firm ones correctly left untagged — there is no legal vertical). Rendered images are the default route; the copy path remains for anything not yet rendered.
- **Slide vault (2026-09-02) — built, schema live, one real entry.** See "Slide vault" above; also gained a real hard delete (2026-09-08), used to remove a round-trip-test fixture entry that had accumulated in the shared vault.
- **Drafting:** draft-from-notes, one clarification round, audience finder (browse + suggest), avails ingestion with `percent_of_avails`, and the Wide Orbit broadcast import. Tier 1 and tier 2 both green.
- **Geo/markets:** target markets seed the avails table (`tests/test_avails_seeding.py`), quick-add builds plan lines from it (`tests/test_quick_add_lines.py`), and `geo_resolver.py` resolves zips/counties/radius with markets pluggable (`tests/test_geo_resolver.py`, `tests/test_geo_defaults.py`). `zips_to_markets`/`market_to_zips` are live against `market_lookup.json.gz` (3,118 counties → 209 of 210 markets, built by `build_market_lookup.py` from three cross-validated public sources, asserted by `tests/test_market_lookup.py`). **Installing is explicit — `market_lookup.install()`** — so `geo_resolver` still registers nothing by itself and reports itself unavailable rather than guessing. **DMA definitions are Nielsen's IP and no authoritative free table exists**, so this one is a working compilation with its vintage and two known gaps (Palm Springs; Connecticut planning regions) recorded in its own provenance block; replacing it is a data change, not a code change. `geo_targeting_roadmap.md` carries the detail and the rest of the plan.
- **Targeting groups (roadmap §D) — all 9 phases done.** `targeting_groups.py` (new, pure) plus `app.py` wiring make `targeting_groups` the source of truth for audience+geo, projected to/from the legacy flat `avails_seed_rows` shape so every existing proposal loads, rebuilds and renders unchanged; a plan row carries a hidden `_group_ids` field so two rows sharing a rendered Geo string never collapse onto each other in the shared-field re-seed, and the same field drives `merge_plan_rows`/`split_plan_row` on the media plan grid without ever touching a group itself. The D2 avails table is a grouped editor — hidden `gid`, Audience, a Markets multiselect (`accept_new_options`), avails, a color swatch (§E, the map, now built — see the next bullet) — fed by a per-group geo-definition expander (Markets/Counties/Zips/Radius through `geo_resolver`, `install_market_lookup()` the one place `market_lookup.install()` is actually called) and by the Audience finder's AND/OR/"New group" builder in place of one flat Add button; the one-custom rule is allow-with-a-warning at both the click and in the persistent "review before generating" panel, computed live from `targeting_groups` every run. The finder also shows a **booking-evidence panel** for the group being built, live against `audience_evidence.py`'s index of the real `audience_usage` log (Supabase, falling back to the committed `audience_usage_ytd.csv`) — exact-match only when true, component familiarity, the weakest pair, overlap-weighted suggestions, per the settled panel order and wording rules. A draft-from-notes audience seeds a `targeting_groups` entry the same way any other seed row does. **Every D2 grid column that can only DISPLAY a group's derived state, never its real structure — the avails figure, Audience terms/op, Markets/geo_def/resolved fields — shares one fold-back test, `_cell_unchanged`**: re-deriving structure from displayed text unconditionally, on every rerun and not just a real edit, silently destroys data the moment the grid redraws — a real bug once (a 2-term AND group collapsed to one verbatim term). A fourth fold-back field gets the fix by construction now instead of needing the hazard rediscovered. Guards: `tests/test_group_merge_split.py`, `tests/test_group_builder.py` (also covers the evidence panel), `tests/test_audience_evidence.py`, `tests/test_draft_regression.py`. **The five hand-run scenarios in `targeting_groups_test_scenarios.md` are now automated too** — `tests/group_scenario_fixtures.py` transcribes three real avails PDFs (Annapolis Cars, Visit Hershey & Harrisburg, Wilmington University; gitignored, real client pricing) into `targeting_groups` data, resolved through the real `app.resolve_group_geography`; `tests/test_group_scenarios.py` asserts row count/ordering/no-collapse/geography/avails-totals against each document's own published total (`--render` also Generates, opens the deck in PowerPoint, and checks the plan table); `tests/test_group_backward_compat.py` is Scenario 5, and is why `rebuild_proposal_deck`'s Timing fallback now matches Generate's (see the proposal-history rule above). Scenario 0's exact inputs (the multi-county semicolon list, radius 21401, and its own zip list) were folded into `tests/test_group_geo_resolution.py` rather than duplicated. **`geo_targeting_roadmap.md` §D carries the full design and incident history** — read it before touching this again.

- **The avails table decoupled from the media plan (2026-08-24) — groups own their Premion Streaming TV line via an explicit `include_in_plan` tick, default off.** Full design and reasoning in "The media plan and the form" above (the ownership rule, the reconciler, the drafting `group_selection`/`group_allocation` schema, the accepted draft-before-any-avails-work limitation). Retargeting/AM/sports/fees stopped fanning out one row per targeting group in the same pass — they were never conceptually per-group, only inherited the fan-out from sharing `_seed_option_rows` with Premion. New session_state: `draft_plan_intent`, `_pending_group_realloc`, `_pending_include_removal`, `_confirmed_group_row_removals`. Backward compat: a group logged before this shipped has no `include_in_plan` key at all — `rehydrate_proposal_into_form` derives it from whether the group's id appears in any stored row's `_group_ids` (locked, so nothing re-derives it), never the new-group default of False; a proposal old enough to predate `targeting_groups` entirely (flat rows only) migrates every group `include_in_plan=True` (every avails row drove a line automatically, pre-groups). Guards: `tests/test_group_plan_selection.py`, `tests/test_group_plan_order_invariance.py`, `tests/test_draft_group_ownership.py`, `tests/test_group_backward_compat.py`'s legacy-proposal case, `tests/test_avails_pdf_wiring.py`. See BACKLOG.md for the intake-area layout fix that removes the draft-before-avails-work gap without anyone needing to know it exists.

- **ZCTA (zip-area) polygons — per state, lazily loaded, and adding a sixth state is a one-line rerun.** The attribution report's heat map shades real zip AREAS, which needed data this repo never had: `geo_crosswalk.json.gz` carries zip POINTS and `map_boundaries.json.gz` carries county/state POLYGONS, and nothing carried zip polygons — which is why the first choropleth shaded centroid dots and couldn't ship. `build_zcta_boundaries.py` fills that gap from `cb_2020_us_zcta520_500k` (the Census *cartographic*, pre-generalized product — 63.6 MiB, against 503 MiB for raw TIGER/Line), Douglas-Peucker-simplified at **0.001°**, stdlib only, reusing `build_map_boundaries.py`'s own shapefile readers and `ROUND_DP`.
  **To add a state: `python build_zcta_boundaries.py --add GA`** (keeps what's already built; `--list` shows current states and sizes; `--states ...` rebuilds an explicit set). Expect roughly **0.3-0.6 MiB gzipped per populous state** — measured: DC 3 KiB, MD 149, VA 440, NC 477, PA 582, so all five current states total 1.65 MiB; a national build would be 14.7 MiB. The source archive caches under `../proposal-builder-assets/geo` like every other geo builder, so a second state costs a few seconds, not another download.
  **The payload is deliberately NOT in `map_boundaries.json.gz`.** Every targeting-map render loads that file; only the report's choropleth needs ZCTAs, and the proposal builder must not pay ~1.65 MiB it never draws. `targeting_map._zcta_state()` loads `zcta_boundaries/zcta_<ST>.json.gz` lazily, one file per state, `lru_cache`d — a two-state campaign reads two files once each. Same silent-degrade contract as `_boundaries`/`_places`: a missing state file is `None`, and the choropleth falls back to centroid dots for those zips **with the count reported as a warning**, never a crash. A large missing count is the tell that a state needs building. **The built `zcta_boundaries/*.json.gz` files are COMMITTED, not generated at deploy.** They were briefly gitignored as "derived data the builder can regenerate" — which is true locally and wrong for production: Streamlit Cloud deploys from git and never runs a builder, so ignoring them would have shipped a heat map that silently fell back to centroid dots, the exact failure the ZCTA work had just closed. "Lazy load" is a runtime-cost rule, not a packaging one. The repo's committed geo data goes 1.39 → 2.97 MiB with these in it, in line with what was already there. The tolerance was chosen by pixel math, not feel — see the builder's own docstring, and `DECISIONS.md` for why the simplifier is iterative.

- **Zip/map builder (roadmap §E) — built.** `targeting_map.py` draws every targeting group's `resolved_zips` as colored dots at their crosswalk centroids (never a filled region — no zip/county/state boundary data exists anywhere in this repo, and a convex hull would shade territory nobody targeted), with a legend, using Pillow alone — no new dependency, no paid service, nothing platform-specific (Pillow's own bundled font, no filesystem lookup). Its own page (also reachable from a group's geo expander), reading `targeting_groups` directly — "draws what already exists," no resolution of its own — with a copyable/exportable zip list per group alongside the picture. On the slide, `assembly.targeting_map_region`/`place_targeting_map` derive the stock-image footprint from the avails table's own geometry (`_floor_below`, generalized out of `_content_floor` so both share one measurement) and do nothing at all when no group has resolved zips — confirmed the stock photo is then byte-for-byte what it was before. **Found and fixed along the way:** `rehydrate_proposal_into_form` wrote `avails_seed_rows` and `targeting_groups` together without setting the marker `sync_targeting_groups` uses to know they already agree, so the very next sync call re-derived groups from the flat rows and silently wiped `resolved_zips`/`geo_def` kind on every "Load into form" of an already-resolved proposal — invisible until the map depended on it surviving, caught by the byte-identical Scenario 5 check going red the moment it started reading `resolved_zips`. **Found, not fixed here:** the avails table has no row-height sizing pass at all (unlike the media plan table) — real numbers, confirmed independent of the map: Wilmington's 5-row, ten-county-Geo table renders to **12.92in on a 7.5in slide**. `geo_targeting_roadmap.md` §E has the full numbers and reasoning; the check is left red on purpose. Guards: `tests/test_targeting_map.py`, `tests/test_group_scenarios.py --render`, `tests/test_group_geo_def_compat.py`.

- **Color follows the audience, not the group — built.** A targeting group's `color` used to be assigned once, round-robin, per group; now every group under one audience shares that audience's color (`targeting_groups.audience_cascade_color`, consulted whenever a new group is created — by the finder, an avails-PDF import, or the D2 grid's own "+") until a rep deliberately breaks ONE group out. `targeting_map.legend_entries` is the whole model: groups still painted their audience's own reference color collapse into one legend entry; a group painted a *different* color (bucketed by the color actually painted, not just a `color_locked` flag, so two groups that merely happen to differ are never silently merged either) gets its own entry, "{audience} ({its own geo_label})". **Once any group is broken out, the remaining shared entry is qualified too** (`_qualified_label`) — a bare "Auto Intenders" sitting above "Auto Intenders (Denver)" can't say whether Denver is included, which matters on a document a client signs; every label — qualified, overlap, or multi — is fit to what the legend can actually show (`_fit_legend_label`, measured against the real rendered glyphs, never a character count) by dropping trailing markets and saying "+N more," never by shrinking type or cutting silently, and never by fabricating a count when there's only one real item to begin with (a genuine early bug: a single-item parenthetical still over budget from a long audience name is left alone, not turned into a false "+1 more"). Two *different* audiences whose resolved geography lands in the same county are never flattened into one fill: exactly two clearing the coverage threshold hatch in both real colors (`_fill_overlap_rings`); **three or more get a different, neutral cross-hatch fill** (`_fill_multi_rings`, `_MULTI_FILL_COLOR` — a flat grey outside `GROUP_COLORS`, never derived from an audience actually present) rather than a two-color hatch that would have to silently drop whoever's third. Both hatch fills are built as ONE flat multi-color pattern before a SINGLE `alpha_composite` onto the canvas — compositing the second color as a separate pass on top of an already-composited first was tried and produces a muddy third-hue blend at every stripe, exactly the ambiguity a hatch exists to avoid. **Streamlit has no `ColorColumn`** (confirmed against the installed version and a live search — a long-open feature request, not something declined here): the D2 avails table's Color column is a `SelectboxColumn` constrained to `tg.GROUP_COLORS`, each option prefixed with the closest-reading emoji (no dedicated teal/mauve/pink/grey circle glyph exists, so those four borrow the nearest alternative) — a real hex is always what's stored and drawn; the emoji is display-only. A plain edit to one row's swatch is a single-row, permanent detach, exactly like the Label column's own override, surfaced right in the grid as a disabled "Detached" (🔒) column so a rep can see why a cascade skipped that row without opening the expander below. Pushing a color back OUT to an audience's other rows is a separate, explicit "Apply to all" button (`apply_pending_color_cascade`, queued the same "apply before the grid reads groups this run" way `apply_pending_avails_import_fields` already does) — never a side effect of an ordinary edit — and **it re-attaches an already-detached row too** (clearing `color_locked`, not just matching the color), since it's the only way back from an accidental detach otherwise permanent by design. Guards: `tests/test_targeting_map.py` (legend collapsing, qualification, truncation including the single-item fabrication bug, the 3-way-vs-2-way distinction, and that two groups sharing one audience never "overlap" with themselves) and `tests/test_color_lifecycle.py` (the full cascade → detach → holds → re-attach → delete → add lifecycle through the real D2 grid, split across two AppTest sessions for the delete/add half — confirmed-by-direct-repro AppTest limitation, unrelated to this feature, rejects a brand-new button key in a session where that exact dynamically-keyed button set had shrunk to zero and grown back).

- **Avails PDF importer (roadmap §F) — built.** `avails_pdf_import.py` (new, pure) parses a Premion Salesforce avails export into one audience-geography group per block, classified into all four geo forms (DMA bare name, named zip option, radius with optional bracketed origin, county option), matched term-for-term against the three real documents' independent hand-transcription in `tests/group_scenario_fixtures.py`. Two entry points share one importer (`app.render_avails_pdf_uploader`, `app.apply_avails_import`) — the intake area at the top of the page, and beside the D2 avails table (the old third entry point, gated on drafted notes mentioning "avail," was retired by the UX sweep once the intake slot made it unconditional and redundant) — with precedence rep edits > document > draft > defaults, conflicts flagged in the report rather than resolved silently, and a re-uploaded RFPID refused rather than duplicated. **Two real findings, both documented in `geo_targeting_roadmap.md` §F:** the roadmap's original daily-rate/"30-day-equivalent" design for Max Monthly Avails was never actually needed (it disagrees with the already-shipped `avails_to_display` convention for a flight that doesn't align to calendar months) and isn't what got built; and `targeting_groups.parse_expression` needed to accept a lone bracketed audience term (Salesforce wraps even a single term in parens) to stay the one shared parser rather than needing a second one in the importer. Guards: `tests/test_avails_pdf_import.py` (the pure parser against all four real PDFs, gitignored, SKIPs without them), `tests/test_avails_pdf_wiring.py` (both entry points through the real form).

- **A persistent, blank "no target market picked" placeholder group — live bug, found from real use.** `avails_rows_for_markets` seeds one starter row (blank Audience, the originating market's Geo, zero avails) whenever no target market is picked — that's D2's normal empty-table starting point — but `seed_rows_to_groups` promoted it into a genuine, permanent `targeting_groups` entry, and both places that ADD groups on top of what's there (`_finish_avails_import`, `_add_segment_to_group`) appended rather than replaced, unlike picking a real target market (which rebuilds the list from scratch and discards it for free). The row and the group it seeds now carry a `_placeholder` marker, round-tripped by `seed_rows_to_groups`/`groups_to_seed_rows` and carried forward by the D2 grid's own fold-back loop (a fourth field added to the existing Audience/Markets/Label/Color "unchanged cell keeps its value" test) — both append sites drop a `_placeholder` group from `existing` before adding real ones. Guard: `tests/test_avails_placeholder_group.py`.
- **The map's `GROUP_COLORS[1]`/`[4]` (orange/red) were too close to tell apart — live bug, found from real use.** Both were desaturated, similarly light warm hues, exactly the pairing that reads as one color at small map-dot sizes. Swapped for tab10's bolder, more saturated `#FF7F0E`/`#D62728`; every other color, and every proposal's already-stored hex (color is a literal string on a group, never re-derived from an index), is untouched. `_COLOR_SWATCH_LABELS` keeps the superseded hexes in a separate `_LEGACY_COLOR_SWATCH_LABELS` fallback (display only, so an old proposal still shows a real color name) rather than in the dict the D2 grid's Color dropdown builds its options from, which would have shown "Orange" twice.
- **Unresolved avails-import notes named the whole document, never the specific group, and a named-zip group's `.unresolved` was dumped one line per zip — live bug, found from real Hershey use.** Naming only the document is indistinguishable when two of a real document's 12 groups hit the same failure; dumping `.unresolved` one-per-zip skipped the calm-vs-warning split `geo_resolver`'s `Resolution.notes`/`.unresolved` shape already exists for (the same split the geo-definition expander applies, per the 2026-08-20 zip-messaging fix). Every note in `apply_avails_import` is now prefixed by that group's own geo/audience label, and the named-zip branch surfaces `.notes` and filters `.unresolved` to genuinely malformed entries only.
- **The per-group collapse above still repeated the same calm fact once per GROUP, and the continuation-page fix made this several times worse.** A real Wilmington import (303 zips across 5 groups, up from 48 before that fix) or Hershey import (12 groups) turned "N zip(s) have no county on file," "N zip(s) matched a county but no market on file," and "N zip(s) span counties in more than one market" into several near-identical report lines for facts that are true of the whole document, not any one group. `_actionable_geo_notes`/`_CALM_GEO_NOTE_MARKERS` strip all three out of every per-group note list; `apply_avails_import` instead unions every zip-originated group's zips into one pool and reports each fact **at most once, aggregated**, in `report["unresolved_internal"]` — never in the rep-facing `unresolved` list at all. Same pass: the document's Attribution field stopped being restated as its own report line (Section D's toggles are the rep's call regardless; the field is still matched against `_AVAILS_ATTRIBUTION_MAP` to set a toggle, just never quoted back). Real Hershey and Wilmington now report exactly one rep-facing line each — the impressions ground-truth line. Guard: `tests/test_avails_import_notes.py`.

**Not started:** Vault slide metadata; Claude-generated Campaign Specs copy
without notes to draft from (still typed by hand if you skip the notes panel);
per-tactic custom month-by-month impressions (the Monthly/Full Flight breakout is
per-option, not a per-row-per-month grid); Vault admin page (phase 2); audience
intelligence (phase 3); a Real Estate vertical (apartments, realtors and property
management are deliberately unmapped today) and a legal one.

Two ratecard rows still have no deck slide and no app option: MLB Home Team
Regular Season ($65, restricted to 14 teams) and NFL Regular Season – ABC MNF
($70); Motorsports ($59) has a viewership slide but no package slide. Each needs
a master-deck slide authored before it can become selectable. There is also no
FOX43/WPMT counterpart to the WUSA9 Broadcast TV Strategy slide, so a Harrisburg
Total TV deck is one slide shorter than a DC one.

**Fixed: a targeting group's Geo cell no longer carries a raw zip/address
list.** `tg.geo_label` used to render a `kind:"zips"` group's Geo as its
raw, comma-joined zip list — a real one runs 50+ zips — which is wrong
content for a client-facing table regardless of whether it technically
fits; found by `tests/test_group_scenarios.py --render` on the Visit
Hershey & Harrisburg scenario and confirmed with
`tests/validate_text_metrics.py`'s declared-vs-PowerPoint comparison (8 of
15 rows rendered taller than declared, table bottom 0.62in past its floor).
**Fixed the label, not the sizer**, per the explicit call on this: a rep's
own `group["name"]` (a new "Label (optional)" field in the geo-definition
expander) wins for every geo kind; absent that, `zips` derives "resolved
market(s) -- N zips" and `radius` derives "Nmi radius of K locations" past
two centers — the full zip/center list stays on the group
(`resolved_zips`/`geo_def`) for wherever it actually belongs, never a plan
table cell. `tests/test_group_scenarios.py --render`'s `grown == 0` check
(via `validate_text_metrics.check_deck`) dropped from 8 grown rows to 1: a
small, separate, ~0.10in residual from the "Full Flight Total (N months)"
label itself wrapping at a cramped font on a 12-row table — a different
root cause (the width `balance_text_columns` now gives the Tactic column,
once Geo's demand dropped), not yet fixed, and the assertion stays red for
it rather than being weakened to pass.

**Fixed: two more independently-drifted generate/rebuild defaults, same
shape as the Timing one.** `rebuild_proposal_deck` and
`rehydrate_proposal_into_form` both used `form.get("proposal_title") or
"CTV Strategy"` — substituting the synthesized default for a REAL,
deliberately-blank title too, not only a genuinely-absent field. Both now
use `form.get("proposal_title", "CTV Strategy")` — the dict-default form,
which only fires when the key itself is absent (a proposal logged before
the field existed). Guard: `tests/test_group_backward_compat.py`.

**Radius mode takes many centers, not one.** The geo-definition expander's
Radius mode is a textarea, one center (zip or street address) per line, at
a shared default radius; a line ending in `", <number>"` overrides the
radius for that line alone (`app.parse_radius_centers`) — an ordinary
address that happens to contain a comma is left whole, recognized only
when the text after the LAST comma parses as a plain number.
`resolve_group_geography` resolves each center with its own
`geo_resolver.radius_to_zips` call (a list of one) rather than one batched
call — the network cost is identical either way (radius_to_zips geocodes
one center at a time internally regardless), and per-center calls are what
make a per-center report possible: a summary line ("20 location(s),
default 10mi (1 with a custom radius), 139 unique zip(s)"), then one line
per center naming its own zip count or that it specifically couldn't be
geocoded — a bad address is never silently absent from the union, and the
union itself dedupes overlapping circles by construction (`set`). Measured
against the real Census Geocoder while building this: ~0.15s per address,
so 20 addresses is a few seconds, not parallelized against a free, keyless,
public API — a `st.spinner` covers the wait rather than a progress bar.
`geo_def["centers"]` stays backward compatible: an un-overridden center is
still a bare string (byte-identical to every radius group stored before
this), only an overridden one becomes `{"center", "miles"}`. Guard:
`tests/test_group_geo_resolution.py` (20 centers, deduplicated union, the
bad-address and per-line-override cases).

**UX sweep (BACKLOG.md) — one intake area for "things a rep has in hand," built.**
The app used to be organized by *where a rep happened to be in the form*: a
collapsed "Draft from notes" expander easy to miss, an avails-PDF uploader
duplicated a third time behind a "notes mention avail" heuristic, a Wide
Orbit uploader buried inside Total TV, a logo uploader in Section A. All
four now live in one un-collapsed "📥 Start here" block at the top,
organized by *what the thing is* instead. Meeting notes can now be uploaded
as a file (`notes_file_import.py`, new and pure — `.txt`/`.pdf`/`.docx`,
the last needing a new pinned dependency, `python-docx`) as well as pasted;
extracted text is written into `draft_notes_input` **before** its own
`st.text_area` renders in the same run, the same "a keyed widget's
session_state wins over value=" discipline everywhere else in this file
depends on, guarded by a `notes_upload_loaded` companion key so a rep's
edit afterward is never silently overwritten by the same upload again.
**Wide Orbit is split, not just moved**: `render_wide_orbit_upload`
(intake, unconditional) parses on upload the same as before;
`render_wide_orbit_summary` (still under Total TV in Section C, the only
context the numbers mean anything in — a real, deliberate prior decision
this respects rather than overrides) reads what the intake upload already
parsed, and points back up at the intake area instead of showing an empty
panel when nothing's been uploaded yet. The avails PDF importer's own
"only when notes mention avail" entry point is retired outright — the
intake slot is unconditional, so the heuristic became redundant, not just
one-of-three. Guards: `tests/test_notes_file_import.py` (the pure
extractor, `.docx` generated on the fly, `.pdf` reusing whatever real
avails PDF already sits at the repo root), `tests/test_intake_area.py`
(all three relocated/new pieces through the real form), and
`tests/test_avails_pdf_wiring.py`'s own entry-point scenario updated for
the new unconditional slot.

- **Flow rework Phases 1–2 (`FLOW_REWORK_PLAN.md`) — landed.** Phase 1: the
  setup band (originating market, flight dates, plan basis, avails mode,
  Total TV) moved above every other section, gating the rest of the page on
  market+flight; `setup_snapshot`/`resolve_setup` are the single capture/
  migration-read pair every proposal (old or new) goes through. Phase 2:
  the whole-month "Active months" multiselect was replaced by per-month
  range controls (`flight_month_ranges`, the single reconciliation owner —
  see "The media plan and the form" above); `format_flight_shorthand`
  renders the day-precise plan-cell/Campaign-Specs string
  (`flight_label` stays the untouched identity key everywhere it's a guard/
  seed key, never display text); the broadcast row's Flight is the Wide
  Orbit schedule's own span, held the same way its Geo already was; and an
  avails-PDF import's figures freeze to the document's own dates rather
  than auto-prorating against the plan's (see the avails-PDF-import rule
  above). Phase 4 has landed (below) — see its own bullet for the reordering
  it superseded from this plan. **The Campaign Specs move itself has now
  landed too (2026-09-04)** — see "Campaign Specs now sits directly under
  Draft from notes" below; it was NOT part of what Phase 5 shipped (see that
  phase's own bullet), and the reason it had been left out turned out not to
  hold (DECISIONS.md's "A redirected item can go stale silently"). The
  progressive-disclosure redesign this note originally grouped it with
  remains queued, not started.

- **Flow rework Phase 3 (`FLOW_REWORK_PLAN.md`) — landed: entities.** A
  targeting group gains `entity_id` (a stable join key, defaults to the
  group's own `id`), `entity_label` (rep-facing name, blank by default,
  never guessed) and `entity_locked` (set once a rep touches the label —
  outranks both the deterministic pass and drafting below). **Entity
  grouping never collapses rows on its own — one ticked avail row is one
  plan line, always**; an entity spanning several rows (a 5mi/10mi radius
  pair, two locations of one brand) is a legitimate multi-line plan shape,
  and only a rep's own `merge_plan_rows` click combines rows. `entities_of`
  and `entity_avails_monthly` (max, not sum) must always be called with an
  already-selected/ticked group list — never a full roster — since rows
  only ever carry more than one `_group_ids` entry via a rep's own merge,
  and `matched_avails_for_row` scopes to exactly that. The D2 grid's
  existing Label column (the Geo-cell override, `group["name"]`) is renamed
  **"Geo Label"**; a new **"Label"** column binds to `entity_label` and is a
  sixth `_cell_unchanged`-guarded fold-back field — an unchanged displayed
  value keeps `entity_label`/`entity_id`/`entity_locked` byte-for-byte, a
  changed one writes the new label onto every group sharing that
  `entity_id` without ever touching `entity_id` itself. Allocation divides
  a stated split by **entity count**, not row count; each entity's one
  computed figure lands on its first selected row in stable order, any
  other selected-but-unmerged row for that entity keeps its $0 — the
  allocation-basis caption under the plan table (`describe_group_allocation`,
  read-only, derived from the draft's own stored intent, never
  back-computed from Cost, and dropped for any row the rep has since
  edited) names which allocation type fired and flags any entity with
  unmerged siblings. `group_allocation` gained `percent_of_avails`
  alongside the four existing waterfall types; **a stated dollar figure
  always outranks an inferred avails percentage for the same entity** —
  deterministic, prompt-level, tested. `avails_pdf_import.infer_entities`
  ties rows together offline at import (R1: nested geography, same
  audience+radius origin, differing radius; R2: nested audience, a DEMO
  age bracket narrowed by gender over the same geography) — scoped to
  exactly the two patterns verified against real documents (Annapolis,
  Plaza), never a guessed general rule; a shared-geography case neither
  rule can key on (Wilmington's three differently-audienced undergrad
  rows) is left ungrouped by design rather than fabricating a tie.
  Drafting's own `group_entities` key is what closes that gap — the model
  names entities `match`ed the same way `group_selection` already resolves
  groups, skipping any group that's `entity_locked` or already tied by the
  deterministic pass. A `show_entity_label` toggle (default off) prefixes a
  group-owned line's Targeting text with its entity's label in both the
  preview and the deck payload (`entity_prefixed_targeting`, applied only
  inside `compute_plan_totals`) — never in the editable grid's own
  `Targeting` cell, which stays a fixed-option `SelectboxColumn` untouched
  by this. Guards: `tests/test_targeting_groups.py`, `tests/test_entity_label.py`,
  `tests/test_entity_allocation.py`, `tests/test_entity_inference.py`,
  `tests/test_group_percent_of_avails.py`, `tests/test_allocation_caption.py`,
  `tests/test_show_label.py`, `tests/test_group_scenarios.py` (Plaza Motors,
  the 2nd gross-markup scenario alongside Annapolis). → `DECISIONS.md`

- **Flow rework Phase 4 (`FLOW_REWORK_PLAN.md`, reordered by a later ruling
  — see the note at the top of that file) — landed, in two independent
  commits.** **4a: per-month flighting moved into the setup band**, behind
  a "Custom flighting" checkbox beside the flight dates (replacing the
  Section E expander and Phase 2's one-line band summary caption, which
  existed only because the control lived elsewhere — one representation of
  the fact now, not two). The band also owns a one-click aggregate
  "Match avails flighting" button next to a divergence notice (moves the
  PLAN's flight to an avails document's window — the opposite direction
  from D2's per-group "Adjust to plan dates," which stays exactly where it
  is for per-group detail); `apply_pending_flight_match_avails` queues it
  the same way every other band-adjacent mutation in this app does. **4b:
  the agency gross-up rebuild** — see "The agency gross-up is a rep-only,
  order-wide ×1.15 multiplier" bullet under "The media plan and the form"
  above for the shipped shape; this bullet exists so the phase reads as
  landed in the status list. Guards: `tests/test_flight_change_guard.py`,
  `tests/test_avails_import_proration.py`, `tests/test_custom_flighting.py`,
  `tests/test_draft_regression.py`, `tests/test_group_scenarios.py`
  (Annapolis/Plaza's gross-markup checks). → `DECISIONS.md`

- **Flow rework Phase 5 (`FLOW_REWORK_PLAN.md`, a narrower design than that
  file's own "Progressive disclosure" section — see the superseding note at
  its top) — landed, 2026-08-30: the setup band became drafting's INPUT,
  not its output.** Market, flight dates and plan basis stopped being facts
  the model had to infer from prose (and sometimes got wrong) and became
  facts it's handed before it reads a word of the notes — a
  drafting-accuracy change wearing a layout change's clothes. The band's
  three intake pieces (notes/draft, avails PDF, Wide Orbit) are now toggles
  that reveal their own input directly beneath themselves when ticked,
  nothing rendered when not, drafting last in the sequence so market/flight/
  basis are already set by the time a rep can click Draft. `DRAFT_JSON_
  SCHEMA_EXAMPLE` no longer has `market`/`flight_start`/`flight_end` fields
  at all — the model is never asked — and `apply_draft_to_form` reads them
  from `st.session_state` instead of writing `market_choice`/`flight_start`/
  `flight_end`/`active_months`/`avails_basis`. A notes date/market that
  disagrees with the band never overwrites it; enforced by the schema's
  absence, not a runtime check. **The gate is unchanged** — still just
  originating market + flight dates, still a hard `return` (Phase 1's own
  rule). The "satisfied declarations" gate idea floated mid-design (blocking
  Generate on an avails/WO upload actually completing) was withdrawn — a
  ticked declaration doesn't mean a file is coming, hand-typed avails and
  rate-card-only broadcast are both real paths — replaced by two
  **non-blocking** warnings at Generate (avails table empty despite the
  toggle; Total TV on with no schedule imported). **Two live bugs found and
  fixed alongside the removal, not patched around:** drafting was
  unconditionally force-writing `avails_basis` to Monthly whenever it built
  its own avails rows, silently overwriting the rep's own plan-basis pick;
  and `apply_draft_to_form`'s `target_dma_choice` write was a wholesale
  replace competing with the avails-import autofill's monotone-add-only
  ledger (`_group_markets_applied`) — a draft naming a new market used to
  silently drop whichever market an avails import had already resolved and
  autofilled. Now a union: a market the avails resolved and the notes didn't
  mention survives a draft naming an unrelated one, both in `target_dma_
  choice` and Campaign Specs Geography. **The drafted-months reconciliation
  incident (Phase 2, "The media plan and the form" above) is now
  structurally impossible, not merely guarded against** — there's no longer
  a second, drafted flight for the form's own to disagree with, since
  drafting never proposes one; the reconciliation branch that incident lived
  in was deleted outright. Guards: `tests/test_form_state.py`,
  `tests/test_avails_reach.py` (the avails_basis assertion inverted — proven
  against both Monthly and Full Flight presets, not just the value drafting
  used to force), `tests/test_draft_regression.py` (`check_band_flight_
  survives_a_disagreeing_draft` replaces the old used-form scenario — the
  band is preset to one flight, the draft dict still carries a different
  one, proving the write is actually gone rather than merely agreeing with
  every fixture's own dates), `tests/test_group_geo_resolution.py` (the
  ledger fix, from drafting's side), `tests/test_group_percent_of_avails.py`,
  `tests/test_draft_live.py` (Tier 2 — `check_common` now asserts market/
  flight_start/flight_end are absent from every drafted response, and that
  no date-resolution phrasing survives in either review list). →
  `DECISIONS.md`

- **Campaign Specs now sits directly under Draft from notes, above the flight-dates gate — landed 2026-09-04 (interface audit finding).** It used to render after D2, on the stated reasoning that its Geography autofill depended on markets the avail resolves (FLOW_REWORK_PLAN.md's own "superseded ordering" note, which redirected the move into Phase 5 and then left it stranded when that phase shipped narrower). That reasoning doesn't hold against the code: Geography's default has only ever come from Section A's Target DMAs / the Setup band's own originating market (`geography_default_text`, `apply_geography_autofill`) — confirmed live, importing a real avails document whose 7 resolved geographies never reached the field. The move itself is a hoist, not a redesign: `apply_geography_autofill`/`target_labels`/`market_label`/`default_geo` all now compute once, early (reading `target_dma_choice` from session_state rather than Section A's own widget return value, since that widget hasn't rendered yet at this point in the script), right before Campaign Specs' own widgets — Section A no longer recomputes any of the three. One accepted nuance: a market a same-run avails import newly resolves doesn't reach Geography's default until the next rerun (Section A's `apply_group_markets_autofill`, unchanged, is what folds it into `target_dma_choice`, and still runs after this point) — invisible in practice, since an avails import always `st.rerun()`s itself. Renamed the D2 grid's own per-row loop variable (`row_audience_cell`, was also `audience_text`) to remove a real latent shadowing hazard the move would otherwise have turned live — Generate's own `AUDIENCE_BULLETS`/`form_json` read the bare name `audience_text` much later in the same function, and would have silently picked up the D2 loop's last row instead of the rep's own field. Guards: tier 1 (377 checks), `tests/test_form_state.py`, `tests/test_setup_resolution.py`, `tests/test_avails_pdf_wiring.py`, `tests/test_group_scenarios.py`, and a live browser check confirming the new render order and that D2's avails table still agrees with Campaign Specs' own Geography field. → DECISIONS.md's "A redirected item can go stale silently" for the general pattern this incident is now the worked example of.

- **The LiveWell deck review (2026-09-01) — entity labels can NAME a single, already-separate row, not just merge several, and a real geo-resolution bug was found underneath it.** `group_entities`'s prompt instruction and `apply_draft_group_entities`'s own `len(matched_ids) >= 2` gate both assumed an entity always spans several rows — wrong for seven LiveWell locations sharing one audience, already correctly separate but each deserving a real name so Targeting doesn't read identically seven times. Both fixed (naming sets `entity_label` alone, never `entity_id` — structurally can't merge). A new deterministic parser, `avails_pdf_import.infer_entity_labels`, covers the unambiguous rows for free (checked against every real document's geo-name format first) and correctly declines — leaves the label blank — whenever two rows would extract the identical name (three of LiveWell's own rows all reduce to "Washington"), rather than guessing an arbitrary disambiguation. "Show Label in plan" gained a one-shot computed default (2+ ticked rows, one audience, 2+ distinct geo labels) that a rep's own later choice always overrides. Color-by-entity was proposed for the same reason (all seven rows render one uninformative color) and declined: `color`/`audience_cascade_color` is also `targeting_map.py`'s own legend-collapsing key, and rekeying it would make the map show seven colors for one audience — worse than the uniformity it replaced. **The real find:** `install_market_lookup()` ran after the intake avails uploader in `main()`'s render order (a side effect of the UX sweep moving intake to the top of the page without moving this call with it) — every zip-originated group imported through intake resolved with no market lookup registered, `resolved_markets` silently empty every time. A live audit of all 22 real logged proposals found 6 with the symptom and one with confirmed, concrete harm: Capital Media (RFPID-266994, generated 2026-08-31) shipped with Campaign Specs Geography reading "Washington, DC DMA" instead of the Baltimore/Salisbury its avails actually targeted. Fixed by moving the call to the top of `main()`. Guards: `tests/test_entity_label.py`, `tests/test_avails_pdf_import.py`, `tests/test_avails_pdf_wiring.py` (functional + a source-order assertion, since `install_market_lookup()`'s `@st.cache_resource` caching is process-wide and makes a revert-and-rerun proof unreliable within one multi-scenario test run), `tests/test_show_label.py`. → `DECISIONS.md`

- **Attribution Report Builder (`ATTRIBUTION_REPORT_PLAN.md`) — a new, separate feature: a rep uploads Premion Website Attribution (+ optional Delivery) exports and gets back a client-ready report deck.** Phases 1-2 landed first (`attribution_import.py`'s pure export parser; the page shell with upload-first and "Build report from proposal" doors; the `advertisers` canonical-client table). **Phase 3 (2026-09-06) — the report master deck + walking skeleton, landed for no-proposal mode.** `report_assembly.py` fills Matt's hand-built `REPORT_MASTER_v0_2.pptx` (13.333×7.5in, same `key:`-in-speaker-notes convention as the proposal master, no anchors needed — every slide already carries its own key) by reusing `assembly.py`'s existing fill primitives; `report_charts.py` renders bar charts and the zip map (via a synthetic one-entry `targeting_groups.new_group` pseudo-group into `targeting_map.render_map` — Phase 5's two-series visitor-vs-targeted overlay will reuse the same trick) as Pillow-only PNGs, deliberately not matplotlib (not an existing dependency) and never a native pptx chart (doesn't survive a future cross-deck slide copy). The Delivery Recap slide (`delivery_set: true` in its notes) is dropped outright, never rendered thin, whenever no delivery file is uploaded. Verified end-to-end against two real fixtures and rendered/screenshotted per the standing rule (`deck_render.render_deck`): Mattress Warehouse (attribution-only) and Cardinal Plumbing (a real attribution+delivery pair — see DECISIONS.md for how "Premion OTT.xlsx" turned out to be Cardinal's file, not MW's, correcting this plan's own prior claim). **Two real layout defects found by rendering, both properly fixed same-day (2026-09-06), not left as stopgaps.** Row counts for the Delivery Recap's two tables are now design rules (top 5 publishers, top 3 creatives — a client reads a top-N list regardless of slide space; `REPORT_MASTER_README.md`/`ATTRIBUTION_REPORT_PLAN.md` corrected from the old, too-large "Top 10"), with `assembly.condense_avails_table` (the proposal master's own measured shrink-to-fit helper, given a new optional `table_shape=` so it can target one of two tables on a slide) as the safety net underneath, not instead of the cap. The Geography tile never truncates a join — up to 2 real markets are listed, 3+ collapses to "N markets" with the full list moved into the recap's own Audience bullets, since a tile holds one short value and a longer one belongs in a different container. The recap's three tiles are now named shapes (`FlightTile`/`ReportPeriodTile`/`GeographyTile`, each with `...Value`/`...Label` children, mirroring `ChartRegion`'s convention) so the blank `FlightTile` deletes itself cleanly instead of rendering as an empty box. **Then v0_3 landed (2026-09-08, report deck version 2, live in Supabase) and closed the phase.** Delivery recap gained a fifth tile (`{{CTV_SHARE}}`, from the OTT-distribution tab — 94.2% MW vs 99.9% Cardinal) and its chart region became the daypart breakdown; `CreativeTable` moved to a NEW `report:delivery_breakdown` slide carrying two CONDITIONAL tables (`DeliveryByGeoTable`/`DeliveryByCreativeTable`, each deleted with its own named header, no reflow) plus a VCR-by-creative chart — the whole delivery SET is dropped off the marker in the notes, never a hardcoded key list, highest-index-first. The URL slide gained `IntentSummaryTable` fed by a deterministic path-pattern intent tagger (`classify_url_intent`, 7 classes including a `homepage` class the real data forced, sub-1% folded for the table but never for `intent_facts()`, Shopify pixel URLs dropped and counted); the block does NOT auto-reflow, so past 4 intent rows the fill shifts the TOP PAGES header and table down by a row height READ from the template and trims URL rows, finding the header geometrically rather than by its text. `TopZipTable` went to five columns behind `ZIP_MIN_SHARE` (1.0%): above-average rate AND real volume, backfilled and marked when fewer than ten qualify — **this rule reproduced the hand-built MW deck's own top-10 table in the same order with impression shares identical to a tenth of a percent.** The zip heat map is now a real ZCTA-polygon choropleth (quantile bins, ground tint, haloed labels) — see the ZCTA bullet above for the data. Correction 3 is tested against the REAL MW pair now (3,104,554 vs 2,341,223), the synthetic stand-in retired once `MW delivery.xlsx` was found. **Phase 4 — the model-facing half — landed 2026-09-08, closing the feature's own plan.** `app.build_attr_draft_prompt`/`call_claude_attr_draft`/`apply_attr_draft` replace the five deterministic placeholders (`report_assembly.py`) with Claude synthesis over `report_assembly.build_facts_payload` -- goals, rep notes, and every computed aggregate, `intent_facts()`'s full-precision classes included. Facts-only contract enforced by `app._attr_payload_numbers` (list lengths and K/M/B-abbreviated big numbers both had to be added after real MW/Cardinal runs flagged them as false positives), not just asked for in the prompt. **The checker's own stated limit: it verifies every emitted number traces to the payload; it cannot verify a traced number wasn't REACHED by arithmetic on other traced numbers** -- a live run once inverted a summed percentage into "1 in 15," genuine new arithmetic the checker missed because "15" coincidentally was also a real, unrelated fact elsewhere in the payload. The prompt's facts-only paragraph now names this exact pattern as its own counter-example, which lowers the odds of the class recurring and does not close it -- verifying a derivation is sound would be a different, larger checker. **The real backstop is a rep reading the narrative before it ships**, which is why a second bug found alongside this one mattered more than it looked: `apply_attr_draft` was silently discarding the model's own `goal_alignment_notes` (its "this disagrees with a goal" / "no data to quantify this" flag) instead of surfacing them -- fixed, and now shown in the UI right after drafting (persisted across the `st.rerun()` that follows, not rendered in the same run and lost) and again at Generate. Verified live on both real datasets with real goals (MW/in-store-visits, Cardinal/service-calls) plus a no-goals edge case. `whats_next_bullets` stays rep-owned -- a draft only pre-fills the box when blank, Generate always reads the box. Guards: `tests/test_attribution_draft.py` (Tier 1, a synthetic non-client fixture -- MW/Cardinal's own real numbers stay out of git the same way their xlsx files do), `tests/test_attribution_draft_live.py` (Tier 2), `tests/test_report_assembly.py`'s `check_phase4_override_plumbing`. Full narrative in `ATTRIBUTION_REPORT_PLAN.md`'s own Phase 4 section. → `DECISIONS.md`'s Attribution Report Builder section, which also covers the `channel_vcr` parser gap, MW's 2.15x projection factor, and the default-argument stubbing trap.

**WAEPA testing (2026-09-08, landed) — the third real fixture, and it found four real things.** A real split-IO multi-RFPID campaign (2 RFPIDs, one campaign) was being rejected by a rule built for a lifetime-rollup shape it can't actually distinguish from a split IO -- replaced with `app.render_rfpid_confirm_gate`, never a hard refusal; `AttributionExport.rfpid_breakdown` carries every RFPID's own figures. Conversions are a real, per-export OPTIONAL layer (`has_conversions` -- the top-line widget tab present AND `> 0`, never inferred from the always-present, always-zero per-dimension columns MW/Cardinal already carry) threaded through `build_facts_payload`/`build_report_deck`'s explicit `include_conversions` -- a 4th Highlights tile -- **template migration landed same-day too**, via `migrate_report_master_v0_4.py` (python-pptx, not by hand: renames the 3 existing tiles to the `ImpressionsTile`/`VisitorsTile`/`RateTile` convention, deep-copies one into a 4th `ConversionsTile` group with matching formatting, repositions all 4 evenly) -- **`REPORT_MASTER_v0_4.pptx` is live in Supabase as `report_deck_versions` id 3, active**, a `conv_rate`/`converted` column on three tables (gracefully degrading via `_fill_named_table`'s own column-count check until those tables are widened -- no template action needed to ship this part), deliberately NOT a 6th zip-table column (Matt's own ruling). `app.clear_attribution_report_state` ("Clear / new report") sweeps every `attr_`-prefixed key, the same boundary `NON_PERSISTABLE_PREFIXES` already draws. The Highlights slide's own narrative quality was a real, separate bug: MW's first pass put 3 of 4 bullets' headline numbers on figures the tiles already show -- four new rules in `build_attr_draft_prompt` (no tile restatement, at most one delivery bullet, prefer comparative findings, at least one goal-connected highlight when goals exist), verified live on both datasets. Guards: `tests/test_attribution_import.py`'s WAEPA/conversions checks, `tests/test_attribution_reports_page.py`'s RFPID/conversions/Clear checks, `tests/test_attribution_draft_live.py`'s highlights checks. Full narrative in `ATTRIBUTION_REPORT_PLAN.md`'s own WAEPA section.
